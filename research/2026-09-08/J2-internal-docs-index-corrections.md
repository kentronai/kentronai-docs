# J2 — adversarial fact-check corrections

**Target:** `J2-internal-docs-index.md`
**Repo:** `~/Desktop/Development/kentron/Receipt`. J2's header says it was read at `c3c16be6`; **`main` is now at `d004ba19`**, two commits later (`c87b2692`, `d004ba19`). Every claim was verified at `c3c16be6` as cited; where `d004ba19` differs it is noted. Working tree at check time: `D LOCAL_SETUP.md`, `?? docs/LOCAL_SETUP.md` — see the dedicated section below.

Verdict key: **confirmed** · **wrong** (with `path:line`) · **unverifiable**.

## Claim table — facts of note and conflicts

| # | Claim (J2 location) | Verdict | Evidence |
|---|---|---|---|
| 1 | §2 Factory/runtime — queue lanes are `chat \| collect \| steer \| follow_up` (`packages/receipt-app/src/modules/job.ts:7-8`) | **confirmed — exact** | `job.ts:7` `QueueCommandLane = "steer" \| "follow_up"`; `:8` `JobLane = "chat" \| "collect" \| QueueCommandLane`. |
| 2 | §2 conflict — `architecture.md:110-113`, `docs/api/streams.md:50-53` and `docs/api/http.md:33` list only `collect/steer/follow_up` | **confirmed** | Root `architecture.md`: "Lanes:" at `:108`, then `steer` `:110`, `collect` `:111`, `follow_up` `:112` (`:113` blank). `streams.md:50-53` "Lanes:" + the same three. `http.md:33` `"lane": "collect\|steer\|follow_up"`. All three omit `chat`. |
| 3 | §2 Toolchain — Postgres 16 with `wal_level=logical` **and `pg_trgm`** (`docker-compose.postgres.yml:13`) | **wrong (the `pg_trgm` half)** — the file has no `pg_trgm` anywhere | `docker-compose.postgres.yml` is 26 lines: `:13` `image: postgres:16-alpine`, `:14` `command: postgres -c wal_level=logical`. Postgres 16 and `wal_level` are right (`:13`/`:14`, not both on `:13`). `pg_trgm` appears in `apps/start/zero/migrations/20260311_add_chat_search_indexes.sql`, `migrations/schema.sql`, `LOCAL_SETUP.md` and `docs/deploy/neon-to-aws-zero-migration.md` — cite one of those instead. |
| 4 | §2 conflict — `docs/GLOSSARY.md:391` says PostgreSQL 17 | **confirmed — exact** | `:391` `- **PostgreSQL (17)** — The system of record store …`. |
| 5 | §2 Toolchain — Resonate floor `>= 0.9.7` enforced at `scripts/start-resonate-runtime.mjs:273`; **[CONFLICT]** `deploy/Dockerfile.receipt:5` pins `RESONATE_VERSION=0.9.5`; `deploy/coder/template/build/Dockerfile:51` uses 0.9.8 | **confirmed — all three citations exact** | `start-resonate-runtime.mjs:273` is the thrown message `… Install Resonate >= 0.9.7 or set RESONATE_BIN …`. `Dockerfile.receipt:5` `ARG RESONATE_VERSION=0.9.5`. `coder/template/build/Dockerfile:51` `ARG RESONATE_VERSION=0.9.8`. The §6 verbatim error string also matches character-for-character. |
| 6 | §2 Other conflicts — connector counts: **63 slugs in `catalog.json`** vs 61 / 58 / 62 elsewhere | **confirmed** | `packages/receipt-app/src/integrations/nango/catalog.json` has `schemaVersion: 1` and a `providerSlugs` array of **63** entries, all unique, at both `c3c16be6` and `d004ba19`. (This also settles the "62-connector table" J1 leaves open in `doc/platform/integrations.mdx:101`.) |
| 7 | §4 Naming — **GovSig**: 0 occurrences in docs, code, tests and config | **confirmed** | `git grep -i govsig` at `c3c16be6` returns nothing, and a case-insensitive recursive grep of the entire working tree (including untracked files, excluding `node_modules`) also returns nothing. |
| 8 | §4 Naming (Beetle) — `APP_BRAND_NAME = 'beetle.run'` (`sidebar-organization-menu.tsx:98`), "Beetle Tasks" (`tasks-page.tsx:172`), "Build {integration.name} with Beetle Tasks" (`integrations-page.tsx:544`), "Beetle runners" (`sidebar-computer-capacity.tsx:54`), "You are Beetle, the product chat profile for beetle.run." (`receipt-chat.service.ts:93`) | **confirmed — all five exact** | Verified line by line at `c3c16be6`. `tasks-nav.config.tsx:16` `title: 'Beetle Tasks'` is a sixth, uncited instance. |
| 9 | §4 Naming (Beetle) — `"Beetle Chat" (chat-sidebar.tsx:160)` listed among shipped UI strings | **wrong** — `chat-sidebar.tsx:160` is a **code comment**, not a rendered label | `:160-162` read `// The 88px rail clips "Beetle Chat"; the product prefix is redundant next / // to the message icon, so the rail shows just the destination.` followed by `railLabel: 'Chat'`. The title comes from `en.json:299` = `"chat_sidebar_title": "Chat"` via `CHAT_SIDEBAR_TITLE` at `:66`. A non-markdown repo grep for `Beetle Chat` returns only that comment plus one test fixture (`app-sidebar-nav.config.test.ts:155`). **J1 §3.B is right and J2 is wrong here** — remove "Beetle Chat" from the UI-strings list in §4 and from §6, or relabel it as a comment/test artifact. |
| 10 | §4 Naming (Kentron) — `ADMIN_EMAIL_DOMAIN = 'kentron.ai'` (`admin-access.server.ts:19-46`); `docs.kentron.ai/introduction` hard-coded (`app-sidebar.tsx:57`); marketing hostnames (`routes/index.tsx:15`); `RECEIPT_LEGACY_PUBLIC_ORIGINS` (`auth.service.ts:454`); `brandLabel="beetle.run"` (`landing-page.tsx:792`) | **confirmed — all five exact** | `admin-access.server.ts:19` `export const ADMIN_EMAIL_DOMAIN = 'kentron.ai'`; `app-sidebar.tsx:57` `const DOCS_URL = 'https://docs.kentron.ai/introduction'`; `routes/index.tsx:15` `MARKETING_LANDING_HOSTNAMES = new Set(['beetle.run','www.beetle.run'])`; `auth.service.ts:454` `process.env.RECEIPT_LEGACY_PUBLIC_ORIGINS`; `landing-page.tsx:792` `brandLabel="beetle.run"`. |
| 11 | §4 Naming (Kentron) — `app.kentron.ai` is baked into the public CLI via `RECEIPT_CLI_DEFAULT_PROD_GATEWAY_URL`, cited to `docs/receipt-cli.md:59-61` | **wrong (citation)** — `:59-61` names `RECEIPT_CONNECT_PUBLIC_GATEWAY_URL`, not `RECEIPT_CLI_DEFAULT_PROD_GATEWAY_URL` | `docs/receipt-cli.md:59-60` says release binaries resolve `https://app.kentron.ai` on their own via `scripts/build-receipt-cli-release.sh`; `:61` then introduces `RECEIPT_CONNECT_PUBLIC_GATEWAY_URL`. The variable J2 names is documented at **`docs/receipt-cli.md:248`** and lives in code at `receipt-connect-command-proxy.ts:56` (`declare const`) and `scripts/build-receipt-cli-release.sh:9,107`. The underlying fact is right; the line pointer is not. |
| 12 | §4 Naming — **Singularity**: 0 docs, EE-only staff route group `apps/start/src/routes/(ee)/singularity/` backed by `apps/start/src/ee/singularity/*`; **Ironclad**: route `routes/(app)/_layout/ironclad/route.tsx` plus "hosted Factory/Ironclad path" at `scripts/deploy-aws.mjs:1050,1057` | **confirmed** | Zero `singularity` hits in any `*.md`. Route files `routes/(ee)/singularity/_layout/{route,index}.tsx` and `_layout/orgs/$organizationId.tsx` exist, backed by `src/ee/singularity/backend/*` and `components/*`. `deploy-aws.mjs:1050` and `:1057` both contain the literal phrase "hosted Factory/Ironclad path". `tasks-page.tsx:105` also puts `'ironclad'` in `TASK_CHANNELS`, which supports J2's open question 3 that it aliases Tasks. |
| 13 | §2 Ordering rules — "Zero migrations are timestamped SQL files (**51** at HEAD under `apps/start/zero/migrations`)" | **wrong** — **50** timestamped migrations; 51 is the total file count including `schema.sql` | `git ls-tree apps/start/zero/migrations` at `c3c16be6` (and `d004ba19`) returns 51 paths, of which 50 match `^\d+_.+\.sql$`. J1's "→ 50" correction of `doc/self-hosting/database-and-migrations.mdx:72` is the accurate figure; J2's 51 would re-introduce the same class of error in the other direction. |
| 14 | §2 Environment — runtime knobs `JOB_LEASE_MS` 300000, `CODEX_JOB_LEASE_MS` 900000, `FACTORY_CONTROL_JOB_LEASE_MS` 900000 (`adapters/resonate-config.ts:23,107-108`); `CODEX_JOB_CONCURRENCY` 1, `ORCHESTRATION_JOB_CONCURRENCY` 1, `CHAT_JOB_CONCURRENCY` 4 (`adapters/resonate-runtime.ts:102-106`) | **confirmed — all six defaults and both citations exact** | `resonate-config.ts:23` `parsePositiveInt(process.env.FACTORY_CONTROL_JOB_LEASE_MS, 900_000)`; `:107` `JOB_LEASE_MS, 300_000`; `:108` `CODEX_JOB_LEASE_MS, 900_000`. `resonate-runtime.ts:102/104/106` the three concurrency defaults 1/1/4. |
| 15 | §2 conflict — `docs/api/config.md:7-14` lists `JOB_POLL_MS=100`, `JOB_LEASE_MS=30000`, `JOB_CONCURRENCY=2`, `JOB_LEASE_GRACE_MS`, `JOB_WORKER_ID`, `HEARTBEAT_<AGENT>_INTERVAL_MS` — "**none of those except `JOB_LEASE_MS`** are read from `process.env` in `packages/receipt-app/src`" | **wrong (one exception)** — `HEARTBEAT_<AGENT>_INTERVAL_MS` **is** read | The `config.md:7-14` table reproduces exactly, and `JOB_POLL_MS`, `JOB_LEASE_GRACE_MS`, `JOB_WORKER_ID` and a bare `JOB_CONCURRENCY` genuinely have zero `process.env` reads. But `packages/receipt-app/src/adapters/heartbeat.ts:34-37` is `parseHeartbeatSpecsFromEnv(env)` scanning every key against `/^HEARTBEAT_(\w+)_INTERVAL_MS$/`, with a 1000 ms floor at `:43` — which is exactly what `config.md:14` documents. Reword the conflict to exclude `HEARTBEAT_*`. |
| 16 | §2 conflict — Teams service env is `CLIENT_ID`, `CLIENT_SECRET`, `TENANT_ID` (`apps/teams/server.ts:19`), while `docs/ms-teams-testing-setup.md:37-39` says `TEAMS_CLIENT_ID` etc. | **confirmed — both citations exact** | `apps/teams/server.ts:19` reads the three unprefixed names. `ms-teams-testing-setup.md:37-39` is the `TEAMS_CLIENT_ID` / `TEAMS_CLIENT_SECRET` / `TEAMS_TENANT_ID` block. |
| 17 | §2 Factory — `FactoryExecutionPath` has exactly one value `"computer"`, only provider `opensandbox`, no non-sandboxed fallback (`modules/factory/types.ts:87-97`) | **confirmed — exact** | `:87` `export type FactoryExecutionPath = "computer";`; `:89-91` the comment ruling out a selectable local/host path; `:92-94` `FACTORY_COMPUTER_EXECUTION_TARGETS = ["opensandbox"]`. |
| 18 | §2 Public CLI — session JWT lives 12 hours (`services/receipt-connect-auth-token.ts:24`) | **confirmed — exact** | `:24` `const DEFAULT_RECEIPT_CONNECT_JWT_TTL_SECONDS = 12 * 60 * 60;`. |
| 19 | §2 Platform — Slack scopes `app_mentions:read, channels:history, chat:write, reactions:write, users:read, users:read.email` (`packages/receipt-core/src/slack.ts:4-9`); the email-less workspace message at `apps/slack/server.ts:599` | **confirmed — both exact** | `slack.ts:3-10` is `SLACK_INSTALL_SCOPES` with those six in that order (`:4-9`). `apps/slack/server.ts:599` carries the quoted string verbatim, including the backticked `users:read.email`. |
| 20 | §2 Environment — Slack service env `SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`, `SLACK_SIGNING_SECRET`, `RECEIPT_SERVER_URL`, `RECEIPT_WEB_URL`, optional `SLACK_PROGRESS_UPDATE_INTERVAL_MS` (20000), `SLACK_MAX_PROGRESS_MESSAGES_PER_RUN` (12), `SLACK_DEFAULT_PROFILE_ID` (`receipt`) — cited `apps/slack/server.ts:96-105` | **confirmed (content); citation 3 lines short** | `:96-98` the three secrets, `:99` `RECEIPT_SERVER_URL`, `:103` profile default `'receipt'`, `:104` 20000, `:105` 12 — all exact. `RECEIPT_WEB_URL` is at **`:108`**, outside the cited range. Widen to `:96-108`. |
| 21 | §2 Toolchain / Ordering — Bun pinned to `1.3.12` by `.bun-version`, Node `24` by `.nvmrc`; Zero image default `rocicorp/zero:1.5.0` (`sst.config.ts:421`); Nango `hosted-0.69.48` (`deploy/Dockerfile.nango:1`) and CLI `0.69.48` (`nango-integrations/package.json:14`); `validate:stack` refuses below 8 GiB free disk (`scripts/validate-stack.sh:72`) | **confirmed — all five exact** | `.bun-version` = `1.3.12`; `.nvmrc` = `24`; `sst.config.ts:421` `process.env.ZERO_IMAGE ?? "rocicorp/zero:1.5.0"`; `Dockerfile.nango:1` `FROM nangohq/nango-server:hosted-0.69.48`; `nango-integrations/package.json:14` `"nango": "0.69.48"`; `validate-stack.sh:72` `VALIDATE_STACK_MIN_FREE_DISK_MB:-8192`. |
| 22 | §2 Environment — model defaults `gpt-5.6-luna` (`adapters/openai.ts:12`), task Codex `gpt-5.6-luna` (`factory-service-config.ts:8`), objective supervisor `gpt-5.6-terra` (`objective-supervisor-runner.ts:111`); `.receipt/config.json` sets `maxActiveTasks: 20`, `maxTaskRuns: 50`, `maxCandidatePassesPerTask: 4`, `maxObjectiveMinutes: 1440`, `mutationCooldownMs: 15000` | **confirmed — all eight values exact** | `openai.ts:12` `DEFAULT_OPENAI_MODEL = "gpt-5.6-luna"`; `factory-service-config.ts:8` `DEFAULT_FACTORY_TASK_CODEX_MODEL = "gpt-5.6-luna"`; `objective-supervisor-runner.ts:111` `DEFAULT_FACTORY_OBJECTIVE_SUPERVISOR_MODEL = "gpt-5.6-terra"`; the five `.receipt/config.json` numbers all match. |
| 23 | §2 Public CLI / §5 — `v0.1.0-preview.6` cannot onboard a fresh machine; `preview.7` bakes the origin and adds `login`/`logout`/read-only `doctor`, but as of 2026-09-07 was **not yet published** (`docs/receipt-cli.md:20-28`) | **confirmed (as a repo claim)** | `docs/receipt-cli.md:20-28` states it verbatim, including "Until the installer default is bumped, install it explicitly with `RECEIPT_CLI_VERSION`". Whether a release has since shipped is **unverifiable** here — `install.sh` lives in the external `kentronai/receipt-cli` repo. Both reports correctly leave this open. |
| 24 | §1.1 / §5 — `README.md:39` promises a React Native app that does not exist; `README.md:127` lists an absent `reference/`; `README.md:107` documents RDS logical replication | **confirmed — all three exact** | `:39` "React Native mobile app currently in development (coming soon)"; `:107` the RDS parameter-group sentence; `:127` "`reference/` - Upstream/reference snapshots (not active app code)". No `reference/` directory exists at `c3c16be6`. |
| 25 | §1.6 — there are **25** `skills/*/SKILL.md` files, with the listed sizes | **confirmed** | The 25 directory names match the repo set exactly — no extras, none missing. All ten spot-checked sizes match to the stated decimal (KB = 1000 bytes). Six further `SKILL.md` files exist under `.cursor/skills/` and are correctly excluded from the count. |

## Claim table — `skills/*/SKILL.md` publish-status classification

Ten files opened and read at `c3c16be6` (working tree byte-identical). Sensitive values are described, never reproduced.

| # | Skill (claimed status) | Verdict | What is actually in the file |
|---|---|---|---|
| 26 | `factory-aws-prod-runbook` — **DO NOT PUBLISH**; note "Account id, cluster name, named actor" | **confirmed, and the note understates it** | A real-looking (non-placeholder) 12-digit AWS account id at `:36`; a concrete SST cluster log-group prefix at `:292` and literal ECS service names at `:283`; a staff given name at `:62,:65,:152,:157,:259,:261`; and — not flagged by J2 — **the person's full given + surname embedded as a SQL literal at `:195`**, plus three named AWS CLI profiles at `:23,:30-31,:123` and secret *names* at `:102,:134`. Highest-risk file in the set by a wide margin. |
| 27 | `factory-aws-rds-objective-debug` — **DO NOT PUBLISH**; note "Named staff actor" | **confirmed** | Staff given name ×7 at `:51` (a whole section heading), `:56`, `:58`, `:60` (embedded in fixture ids `user_<given>` / `org_<given>`), `:64`, `:67`. Also an AWS profile codename at `:29`. No account/cluster/instance ids — the classification rests entirely on the named actor, correctly. |
| 28 | `factory-prod-run-debug` — **DO NOT PUBLISH**; staleness note **blank** | **confirmed as a status; the note is wrong by omission** | Same staff given name at `:21,:23,:26`; two AWS profile names at `:18,:115,:118`; ECS role service names at `:46,:49-52,:104`. No account ids and no literal log groups (all `<placeholders>`). The note should read "Named staff actor; AWS profile names" as it does for rows 27 and 29. **Bonus:** J2's own citation `:48-53` for `RECEIPT_PROCESS_ROLE=driver\|worker-control\|worker-chat\|worker-codex` is **verbatim accurate** — `:48` the lead-in, `:49-52` the four mappings, `:53` the consequence. |
| 29 | `factory-agent-cli` — **CARE**; note "Named staff actor default" | **fact confirmed; classification inconsistent** | Staff given name at `:147` (single occurrence, no surname, no ids); AWS wrappers `:74-108`; Zero projection checks `:110-130`; AWS profile codename at `:97,:153`. The content claims are all true. But rows 27/28/31 are DO-NOT-PUBLISH on the strength of the same given name, so CARE here is not defensible on a consistent rule — J2 should say what distinguishes them (here: no full name, no fixture ids, no account id). |
| 30 | `receipt-connect-cli-prod-debug` — **CARE**; note "Names an IAM user" | **under-classified** | `:126` names a shared internal IAM user **and** states a dated missing-permission finding ("lacked `ssm:SendCommand` and `ssm:GetCommandInvocation` on 2026-09-07") — an operational-security disclosure in itself; a second staff given name at `:160`. All four content claims verified: three-binaries-one-path table `:25-29` (claim at `:22-23`), symptom→cause table `:56-65`, SSM hosted checks `:122-137`, container env names `:146-153`. J2's own citations are exact: `:20-41` spans the three-binaries material; **all four** quoted CLI error strings are inside `:56-65` (`:58`, `:59`, `:60`, `:64`); `:62` really does carry both `session.json` and `session.<target>.json`. Recommend raising to DO NOT PUBLISH for the IAM user + dated permission gap. |
| 31 | `factory-prod-trace-debug` — **DO NOT PUBLISH** | **confirmed** | Staff given name at `:52,:56`; an AWS profile codename hard-coded (not a placeholder) at `:15`; `scripts/prod-debug-trace.mjs` at `:16,:35`. No account/cluster/instance ids. |
| 32 | `deploy-beetle-aws-lite` — **DO NOT PUBLISH** | **over-classified on the stated grounds** | Contains internal deployment stage names (`--stage factory` `:57`, `--stage factory-lite` `:75`), the script name `beetle-deploy:start` `:56`, and a break-glass env-var name `:45`. It contains **no** account id, **no** real AWS profile name (only `<profile>` at `:55,:73`), **no** instance id (`:24` explicitly forbids hard-coding one), and **no** personal name — the only file in the DO-NOT-PUBLISH group with no human name. The status may still be right, but J2's blank note leaves the actual basis (stage/script names) unstated. |
| 33 | `receipt-production-analytics` — **DO NOT PUBLISH** | **confirmed status, weakest basis in the set; note is blank** | The helper name `aws_receipt_production_user_analytics` is at `:22` as claimed. What actually makes it sensitive is the capability, not an identifier: `:37-42` is a recipe for enumerating real production users (display name, stable user id, question/chat/integration counts, last-question timestamp), plus stage name `factory-lite` at `:13,:26`. `:44` forbids reporting emails/message text/credentials. No account ids, no profile names, no personal names. |
| 34 | `factory-infrastructure-aws` — **SAFE**; note 'Still says "profile is `infrastructure`"' | **confirmed on both axes** | `:8` verbatim: "when the active Factory profile is `infrastructure`…", reinforced at `:3`. The staleness is real: `profiles/` now contains only `receipt`, and `factory-chat-profiles.ts:557` folds infrastructure into one voice. Sensitivity is genuinely nil — zero account ids, AWS profile names, host identifiers or human names; `infrastructure` here is a Factory persona id, not an AWS profile. |
| 35 | `factory-organization-skill-catalog` — J2 cites `:14-25` for "org skills mount at `receipt/skills/organization/` with `receipt/skills/index.json`" | **confirmed** | `:14` names `receipt/skills/index.json`; `:24` names both `receipt/skills/index.json` and `receipt/skills/organization/` as controller-owned read-only context. The framing sentence about mounting is at `:8`, just outside the range, but both cited paths are inside it. |

**Cross-cutting finding for §1.6:** the "resolve the named staff actor's production account by default" instruction is a *repo-wide pattern* present in six of the ten files inspected (rows 26, 27, 28, 29, 30, 31), yet J2's Staleness column flags it on only three. It should be called out once as a systemic issue to remediate at source, and the CARE/DO-NOT-PUBLISH split across those six should be made rule-based rather than per-file.

## `LOCAL_SETUP.md` — which path the report should cite

**State of the working tree:** `git status` shows `D LOCAL_SETUP.md` (staged/unstaged deletion of the tracked root file) and `?? docs/LOCAL_SETUP.md` (untracked). The root file **is gone from disk**. The two are byte-identical: both 1,275 lines, md5 `f58f9361…`, so every line citation J2 makes resolves correctly against the untracked copy.

**The problem:** `docs/LOCAL_SETUP.md` does not exist at **any** commit, including `d004ba19`. A reader who checks out the cited HEAD and follows a citation such as `docs/LOCAL_SETUP.md:1050` finds no file. Conversely the tracked path `LOCAL_SETUP.md` exists at HEAD but not on disk. J2 has ~40 citations to the `docs/` path and one honest acknowledgement buried in open question 12 and in §"Changes since 41baea75" ("new, 1,275 lines; now the untracked `docs/LOCAL_SETUP.md`").

**What the report should cite:** `LOCAL_SETUP.md` (repository root) — the path that resolves at the cited commit — with a one-line note in §1.2 and at first use: *"tracked at the repository root at `d004ba19`; an uncommitted working-tree move to `docs/LOCAL_SETUP.md` is in progress, same content, same line numbers."* Do not silently rewrite the citations to `docs/` unless and until the move is committed. Open question 12 ("Should `docs/LOCAL_SETUP.md` be committed at its new path?") should be promoted out of the open-questions list into the inventory row, because it currently makes the report's single most-cited source unresolvable.

Spot-checked line citations against the untracked file — all land on the claimed content: `:107` (`./bunw`), `:197` (only Factory execution path is `computer`), `:229-238` (the three crash-on-import variables `BETTER_AUTH_SECRET`/`BETTER_AUTH_URL`/`ZERO_UPSTREAM_DB`), `:279` (model-key resolution), `:349` (i18n before `db:reset`), `:452` (Unsupported provider message), `:519` (three entry points), `:628` (why step 1 is mandatory), `:673` (`start_all.ready  Local stack is up`), `:735` (never `kill -9`), `:790` (destructive stop), `:835`/`:847` (reload matrix, `VITE_*` rebuild), `:863` (no backfill), `:889` (cannot run concurrently), `:912` (`/readyz`), `:931` (in-repo doctor), `:1050-1061` (fourteen ports; `BETTER_AUTH_URL` must match the browsed origin), `:1095` (`Platform-funded OpenAI access is unavailable`), `:1154`/`:1162`/`:1167` (DEVELOPMENT.md gaps, `.env.example` §4, `AUTH_DEV_EMAIL_OTP_TO_CONSOLE`), `:1243`/`:1249` (resonate resolution, codex version).

## Leak scan — `J2-internal-docs-index.md`

| Pattern | Hits | Assessment |
|---|---|---|
| 12-digit numbers | 0 | Clean |
| `arn:aws` | 0 | Clean |
| `i-[0-9a-f]{8,17}` | 0 | Clean |
| Public IPv4 | 0 | Clean |
| `/Users/<name>` | 0 | Clean |
| `*.ts.net` | 0 | Clean |
| Given names of real people | 0 | Checked against the repo's actual commit authors and the validator's banned identifiers — zero hits. J2 consistently writes "a named staff member's account" and "Named staff actor" instead of the name. **Good practice, correctly applied.** |
| **Internal AWS profile / deploy resource names** | **1 block (`:331`)** | **Flag.** §4's Beetle row prints, in the clear: `AWS profile `beetle``, `CodeBuild `beetle-deploy``, `BEETLE_DEPLOY_STAGE`, ``beetle-deploy:*`` scripts, and `beetle.run`. These are exactly the tokens the docs-site validator rejects (`doc/scripts/check-docs.mjs:122-123`). J2 itself says at `:389` "Never publish … AWS profile names", so the report is self-aware — but any writer copying the §4 table into a page will trip the harness. Recommend replacing the "Operator identifiers" sentence with "internal AWS profile, CodeBuild project and deploy-stage names exist under the Beetle name (not reproduced here)". |
| Additional | `:44` cites `AGENTS.md:132-140` as "a named staff member's account" | Correctly redacted; no change needed. |

**Verdict: no personal-data or credential leaks.** One publication-hygiene issue: the internal deploy-resource names at `:331` must not be carried into the public docs.

## Missing or too-thin topics

1. **The two commits after `c3c16be6` are not covered**, and both change facts J2 states. `c87b2692` fixes an Agent Registry autonomy filter that "never opened" (so the L1–L4 surface J2 lists as reachable was in fact inert at the cited commit), adds `agent-autonomy-levels.ts` as the single source of the level definitions, changes EC2 ownership resolution, and rewrites the workspace-invite flow so the dialog surfaces an invitation link when no mail transport is configured. `d004ba19` adds a first-class Skills composer chip (`prompt-input-skills-menu.tsx`). §5's feature-classification table and the "Changes since 41baea75" narrative both need extending.
2. **No inventory of `.cursor/skills/`.** Six further `SKILL.md` files live there (`web-design-guidelines`, `building-components`, `use-ai-sdk`, `vercel-react-best-practices`, `adding-sidebar-sections`, `zero-sync`). Excluding them from the 25 is right, but their existence should be stated so a reader does not think the repo has 25 skills total.
3. **The publish-status rubric is not operationalised.** §0 defines SAFE / CARE / DO NOT PUBLISH in prose, but the ten files inspected show the rule being applied inconsistently (row 29 vs rows 27/28/31; row 30 under-classified; rows 32/33 with blank notes). A short decision table — "a personal name ⇒ DO NOT PUBLISH; an IAM user or account id ⇒ DO NOT PUBLISH; internal stage/profile names only ⇒ CARE" — would make the column reproducible and would change at least three of the 25 rows.
4. **No verification pass on the `prior corpus` rows.** Six entries in §5 (guardrail groups, "Require organization provider key", plan gating, 2FA, tool-policy claims) cite "prior corpus `01`/`02`/`03`" rather than source. Guardrails are in fact re-verifiable in one grep — `applyGuardrails`/`guardPrompt`/`guardResponse`/`guardToolResult` have **zero** callers outside `guardrail-enforcement.service.ts` at `c3c16be6` — and should be cited to code, not to the corpus. J2's own §5 note "re-verify `guardrail` enforcement call sites" can be closed.
5. **Open questions 4, 5, 6, 7 and 11 are all answerable with a single grep each** and are left open: which worker ids `POST /agents/:id/jobs` accepts, where `receipt new` scaffolds, whether `--profile` still takes `infrastructure`/`software` (row 34 shows the `profiles/` directory now holds only `receipt`), the org-enablement DB write, and whether `docker:dev:up`/`docker:prod:up` survive in `package.json`.
6. **No per-document line counts or last-modified dates in §1.2-§1.5**, only sizes — which makes the "staleness signals" column hard to re-check and is the likely origin of the `51`-vs-`50` migration slip.
7. **The marketing-claims table is explicitly de-facto** (J2 says no claim list was supplied). That is honest, but it means the "supported / partial / unsupported" verdicts should not be quoted as an assessment of any actual marketing copy.
