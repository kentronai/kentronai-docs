### v2:e0b7b4e6dcc7193aec92da0679af5741432acd54f7983fd4ac5e521cf90b7c16 :: research/2026-09-08/B-skills.md
Receipt has three "skill" tiers that share the SKILL.md convention but nothing else: organization skills (admin-uploaded, receipt-backed, mounted into runs), 25 checked-in repo skills (a controller-selected subset copied into an isolated CODEX_HOME), and a 40-entry Python helper catalog that skills reference but which is code, not instructions. There is no plugin concept anywhere.

Organization skills are fully implemented and reachable at /organization/settings/skills (visible rail icon, plus new "+ -> Skills -> Create skill" composer and Cmd+K entries from c3c16be6 that only navigate). Each skill is an event-sourced stream organizations/<org>/skills/<skillId> with six event types (created, version_added, save_acknowledged, enabled_changed, archived, deleted), a deterministic org+slug identity, a manifest-style content hash, and two projections: metadata (Zero-replicated to owners/admins) and bundle bytes (internal only, RCA-385). The frontmatter parser is deliberately tiny: only name (<=120) and description (<=2000) matter, folded/literal blocks are supported, unknown keys are ignored. Surprises: the storage/mount format supports 128-file/1 MiB bundles but every upload path is single-file SKILL.md at 256 KB; Archive exists as a server function but is not rendered anywhere in the UI; a same-content re-upload gives zero feedback; and mount-time guards (256 files/4 MiB/8 MiB) are looser than persistence guards.

Chat authoring is Web-only: the router returns {route:"organization_skill", operation:"draft"|"save"}, the draft is produced by structured output and validated by the same parser as persistence, an explicit "save" reuses the exact reviewed bytes from the last fenced draft, and Slack/Teams fail closed to Factory. Chat cannot edit, enable, or archive an existing skill.

In Factory/computer runs the packet writer snapshots every enabled bundle, mounts them read-only (0444/0555) under receipt/skills/organization/<slug>/ with a catalog at receipt/skills/index.json, keeps them out of CODEX_HOME (RCA-377), rejects them at the patch boundary, and tells Codex via context.md, the prompt, and the always-selected factory-organization-skill-catalog skill to pick "the smallest relevant set". No receipt records which skill was actually read; only skillBundlePaths, repoSkillPaths, the registry hash, and profile.skills.json are recorded. The MCP instructions string was verified to carry workspace, opaque-alias, policy, and GET-only rules, so the "no separate skill needed" doc claim holds. Since 41baea75 only UI labels, composer entry points, router retry hardening, and one new internal runbook skill changed; the core skill code is unchanged.

MARKETING CLAIMS:
- [supported] Skills: organization-defined agent instructions available to Factory and computer runs :: Settings page, receipt events, and the read-only mount at receipt/skills/organization/ with receipt/skills/index.json are all verified in source; snapshotted at packet-write time so only new runs see changes.
- [supported] Skills are versioned and auditable :: created/version_added receipts, content hash on every version, delete keeps the stream and the UI says so verbatim.
- [partial] Create skills from chat :: Web chat only (Slack/Teams fall back to Factory), draft/save of a brand-new single-file skill, org owner/admin only; no edit, enable, disable, or archive from chat.
- [partial] Multi-file skill bundles :: Storage/replay/mount format supports up to 128 files and the detail dialog has a file picker, but every upload path (Settings and chat) accepts exactly one SKILL.md.
- [unsupported] Agents automatically use the right skill :: No ranking or keyword routing by design (RCA-377); Codex decides from the catalog and nothing records which skills were read.
- [unsupported] Plugins :: No plugin concept, registry, manifest, or install flow exists in apps/start, packages/receipt-app, packages/receipt-cli, or docs.
- [n/a] Connectors :: Outside this report's scope; run-shipped skills only reference the receipt connect tool surface.
- [supported] MCP clients need no separate skill because Receipt returns workspace, opaque-alias, policy, and GET-only rules in MCP initialization :: RECEIPT_CONNECT_MCP_INSTRUCTIONS verified verbatim and passed as the server instructions.

OPEN QUESTIONS:
- Is archiveOrganizationSkill meant to stay a server-only function, or should Archive return to the Actions menu? docs/frontend-rift-ownership.md still describes upload/enable/archive actions.
- Are multi-file bundle uploads planned, or should the 128-file/1 MiB format be documented as internal-only?
- Should CLAUDE.md and skills/factory-agent-cli/SKILL.md be updated from .receipt/factory/<taskId>.* to receipt/current/*, and should skills that say .receipt/factory/evidence/ be reconciled with receipt/current/evidence/?
- Is the silent no-op on a same-content Settings re-upload acceptable, or should the UI report 'already saved'?
- Should a skill-read signal be recorded (receipt or remote-skill-preflight.json extension) so that skill usage becomes provable, given marketing interest?
- Which of the 25 repo skills are intended as public reference material? The internal-only classification of 8 runbooks is inferred from embedded deployment details and needs team confirmation before any of skills/ is published.
- docs/integration-factory-receipt.md and receipt-integration-worker describe per-provider SKILL.md files under the Nango slug catalog, but none exist at HEAD; is that convention still intended?
- Should the checked-in receipt/skills/index.json placeholder (always overwritten in task workspaces) be kept in git or mentioned in docs at all?

### v2:761eb4f0a5b52d084153017f7e0fbf941a5c9f042c3ffb7b071aad8da56686c1 :: research/2026-09-08/G-guard.md
At HEAD c3c16be6 the "Guard" story splits cleanly into what is enforced and what is authoring-only. Organization Guardrails remain authoring-only, exactly as at 41baea75: the chat enforcement service (guardPrompt/guardResponse/guardToolResult/applyGuardrails in apps/start/src/lib/backend/chat/services/guardrail-enforcement.service.ts) is complete but a repo-wide grep finds no caller in chat, Factory, or the MCP gateway; the only path that ever evaluates a guardrail is the admin "Test group" dialog. No guardrail-related file changed in the 43 commits since the map. Guardrail evaluations are not recorded anywhere, so the "Monitor only" copy ("Record every match") describes an intent, not a feature. The nine kinds are all fixed regexes and literal term lists with no state, model, or feedback loop, so "threat scanners that learn" is unsupported. The Policies page's four modules (rate limits, budgets, logging redaction, tool approval) persist as receipts but have no consumer; its own copy still calls the Guardrails page "fully connected", which the code contradicts.

What is genuinely enforced: disabled providers/models and Require ZDR (403 policy_denied:* reasons, with a documented BYOK bypass for openai/anthropic keys), Enforce Study Mode, the hidden tool-policy switches, CHAT_REQUIRE_BYOK, and the MCP gateway's read/write classification, connect:write scope requirement, v1/v2 per-connection allowlists (404 for hidden tools), GitHub selected-repository policy, and workspace mutation authority. "Require organization provider key" is a toggle nothing reads. Owner/admin gating is enforced client- and server-side; plan gating returns allowed: true for everything. The Security page (SSO, domains, directory) is a placeholder with disabled buttons and '#' hrefs. The /sessions "Compliance readiness" card is a count-based heuristic whose button only opens the evidence panel.

Cryptographic proof: receipts are SHA-256 hash-chained with prev-hash CAS append, a unique hash index, replay refusal on a broken chain, and a `receipt dst` audit reporting integrity failures. There is no signing, no key material, and no artifact named "GovSig" anywhere in the repo or the doc corpus; the repo's own note says public hashes are tamper detection, never origin authentication. Human approval exists as an engine primitive (human.requested/responded) but no production agent uses it; chat has no tool-approval step; Factory contracts add connect:write whenever a capability is detected. Surprises: archive for guardrail groups exists server-side with no UI entry point; the Prompt Injection "Also inspect tool results" field is never read by its detector; the Agent Registry (new since the map) classifies L1–L4 and risk from cloud metadata and advises on approval gates without enforcing them.

MARKETING CLAIMS:
- [unsupported] Intercept risky agent actions in real time before execution. :: Guardrail enforcement has no caller (guardrail-enforcement.service.ts:82-132). The only pre-execution gate is the MCP gateway allowlist/scope/GitHub policy (receipt-connect-call.ts:1900-1909), which blocks non-allowlisted tool calls; rephrase to that narrower claim if used.
- [unsupported] Threat scanners that continuously learn and adapt. :: Detectors are fixed regexes and literal term lists with no state, model, or feedback loop (guardrail-detectors.ts); the only 'adaptation' is an admin editing allowlists/terms.
- [partial] Detect and block PII leakage, policy violations, and unauthorized data access in a single dashboard. :: PII/secret rules can be authored and exercised in the test dialog but do not run on live traffic; evaluations are not recorded, so no detections dashboard exists; unauthorized-access blocking is real only at the gateway allowlist level.
- [partial] One policy, every AI tool. :: Model/compliance policy applies to every chat request in the org; tool allowlists are per connection; guardrails and the Policies modules apply nowhere. Say 'organization-wide model and compliance policy' instead.
- [partial] Stop risks before they execute. :: True for disallowed gateway actions, disabled providers/models, ZDR (with BYOK bypass), and Study Mode; not true for content risks (guardrails inert) or spend/rate/approval (Policies inert).
- [partial] Auditable agent deployments: cryptographic proof documents every agent decision (GovSig). :: Receipts are SHA-256 hash-chained, CAS-appended, replay-verified, and auditable via `receipt dst`; gateway calls and configuration changes write receipts. There is no signing, no key material, and no 'GovSig' anywhere; model-policy denials and guardrail tests are not receipts. Use 'tamper-evident, hash-chained receipts', never 'signed' or 'GovSig'.

OPEN QUESTIONS:
- Is guardrail enforcement expected to be wired into chat-orchestrator.service.ts before docs ship, or should Guardrails be documented as authoring-only? Either way the Policies page 'fully connected' copy needs correcting.
- Should 'Monitor only' matches be persisted (receipt type or projection) so 'Record every match' becomes true, and where would admins see them?
- Is 'GovSig' a planned signing layer over the hash chain or a marketing label for it? The docs need one answer before using the word.
- 'Require organization provider key': fix enforcement, hide the toggle, or document as not enforced?
- Should the Prompt Injection blockOnToolOutput field be honored by the detector or removed from the descriptor?
- Is guardrail enforcement intended for chat only, or also Factory workers and the MCP gateway?
- Should Factory objectives keep receiving connect:write by default whenever a capability is detected, given the read-only-by-default positioning?
- Will the /sessions 'Run compliance scan' action invoke a real scan job, or should the card be excluded from public docs?
- Guardrail group archive exists server-side with no UI entry point; ship a menu item or drop 'Show archived'?
- Plan gating is universally allowed; is that the launch contract, and how should pricing-page Plus-feature claims be reconciled?

### v2:a71f3c29f29ef68c09c668ff3d3e384a342b615b653429b33c26521e64d3123e :: research/2026-09-08/F-catalog-agent-registry.md
The "Kentron Catalog" positioning is not backed by any single surface. Four unrelated surfaces each hold one piece: the Agent Registry (/agent-registry/registry, tabs Inventory and Dashboard), Global Integrations (/organization/settings/integrations plus a workspace-scoped twin under MCP Gateway), Skills (/organization/settings/skills), and Org Brain (/organization/settings/knowledge-graph) for usage visibility. Plugins are absent.

The Agent Registry, a stub at 41baea75, became across nine commits a receipt-backed cloud discovery inventory, not a registry you register agents into. Owner/admins connect AWS (Nango slug aws-iam), Azure (azure-service-principal, patched into the pinned Nango image), or Google Cloud (google) through a dedicated Receipt Connect connection named 'agent-registry' that Global Integrations hides. Scans append organization.agent_inventory.scan_requested/completed/failed receipts to the stream organizations/<orgId>/agent-inventory and rebuild rows in receipt_entity_projection. AWS scans every enabled region (ec2:DescribeRegions) for Bedrock Agents, AgentCore runtimes, Lambda, Step Functions, EC2, and ECS; GCP scans eight Cloud Asset types per active project; Azure runs one Resource Graph query over nine resource types across all visible subscriptions. Generic compute counts only when names/tags match a keyword regex (AI_SIGNAL, which includes 'factory', 'receipt', 'beetle'). Autonomy is L4 for all generic workloads, L3 for native GCP/Azure agents, and L2/L3/L4 for Bedrock Agents from action-group confirmation settings; L1 is never produced, so the L1 filter is inert. Owner comes from cloud tags or CloudTrail creator identity and cannot be assigned. The ISM-2134/2135 register (purpose, tools, permissions, data access, findings) is populated only for Bedrock Agent, Lambda, and Step Functions via IAM policy introspection; AgentCore, ECS, EC2, GCP, and Azure report a reason string instead. The only row action is "View details". Any org member can view; only owner/admin can connect, scan, refresh, disconnect. A local error boundary ("Agent Registry couldn't load.") exists.

Global Integrations is a ~900-entry directory (test asserts >= 894) of which the audit fixture pins 63 connectable connectors; visible filters are search, six category chips, and All/Connected/Available/Popular; the auth-method select and "Show connected only" switch are DOM-hidden. Per-connection "Manage tools and permissions" allowlists read vs write operations. "Request an integration" files a Beetle development task. No approval, vetting, or dependency data exists anywhere; the only approval concept is a Policies tool-call approval rule. Adoption data exists in Org Brain (objectives per application, connection readiness; polls every 30 s) and the MCP Gateway activity dashboard. Word count by wc -w is 8,161 including path citations and table delimiters.

MARKETING CLAIMS:
- [unsupported] One governed registry for approved connectors, skills, and plugins :: Three separate surfaces (Agent Registry, Global Integrations, Skills) with no shared registry, no approval state, and no plugins at all.
- [partial] with clear ownership :: Agents show an owner read from cloud tags or CloudTrail (else 'Unowned') but it cannot be assigned; connections and skills store a creator id the UI does not present as an owner.
- [unsupported] dependencies :: No dependency model, graph, or view exists between agents, connectors, skills, or tools.
- [partial] access controls :: Owner/admin vs member gating on every surface plus a per-connection read/write operation allowlist ('Manage tools and permissions'); no finer roles or per-connector approvers.
- [partial] usage visibility :: Org Brain shows objectives per application and connection readiness over 30/60/90 days; the gateway dashboard shows calls, success rate, response time, actors. Nothing for skill usage or per-agent invocation.
- [unsupported] Centralized registry with automated approval ensures only vetted connectors reach production :: No approval automation or vetted flag; connectors are connectable if present in the checked-in runtime catalog, and connecting is a direct owner/admin action. 'Request an integration' files a code task.
- [partial] Clear accountability with role-based access rules :: Role rules exist (owner/admin/member). Accountability is limited to the receipt audit trail (every scan, skill, and connection event carries an actor); there is no accountability workflow.
- [unsupported] Real-time visibility into connector dependencies :: No dependency visibility at all; Org Brain polls every 30 seconds and the gateway dashboard refreshes on demand.
- [partial] Real-time visibility into adoption patterns :: Adoption by application (Org Brain) and by tool/connector/actor (Gateway activity) exists, receipt-derived and polled, not real-time push.
- [partial] Real-time visibility into security posture :: Agent Registry risk levels, ISM findings, wildcard-permission detection for three AWS resource types, and connection validity counts; heuristic, best-effort, AWS-weighted; nothing for connectors or skills, and only refreshed by manual scans.

OPEN QUESTIONS:
- What framework do the 'ISM' control numbers (2113, 2133-2136, 2141-2143) refer to, and may they be published? The code says they came from a customer's pasted requirement.
- Is the Agents sidebar area intentionally visible to plain members, given listAgentInventoryAction returns owner identities and IAM findings to any member while only mutations are admin-gated?
- Has a large multi-region AWS account been tested against the deployment's request timeout? Discovery runs synchronously with no total time bound and a stuck 'running' scan has no reaper.
- Should Org Brain be readable by every member (server intent) or owner/admin only (current route guard)?
- Which GCP IAM permissions and Azure RBAC role are required for scans? The repo only shows the APIs called.
- Is there a supported way for an admin to set an explicit AWS region list on the connection, given the aws-iam Nango form has no region field?
- Which name should public docs standardise on: 'Agents', 'Registry', 'Agent Registry', or 'agent inventory'?
- Are the DOM-hidden Integrations filters (Authentication select, 'Show connected only') slated for removal or return?
- Should 'Kentron Catalog' be one page, or three pages plus an overview, given the surfaces have different audiences and navigation homes?

### v2:3b9a1fe40cd95449044ef7b33384da78582d47797f85c1d18237b9816e28cf7d :: research/2026-09-08/C-factory-execution.md
Factory is Receipt's background execution engine: an objective (stream factory/objectives/<id>) is planned into a task DAG by an LLM supervisor at explicit receipt boundaries, executed as jobs through a receipt-backed queue plus Resonate durable promises (receipt.job.driver -> receipt.job.execute), run inside disposable OpenSandbox computers with the Codex CLI as the agent, and settled back into hash-chained receipts. At HEAD c3c16be6 there is exactly one execution path ("computer") and one provider ("opensandbox"); five process roles (api, driver, worker-chat, worker-control, worker-codex) share one image; lanes are chat|collect|steer|follow_up; job statuses queued|leased|running|completed|failed|canceled; and lease/heartbeat/attempt fences yield explicit result statuses (stale_attempt, lease_lost, terminal_state, execution_timeout, settlement_conflict). Three recovery loops (queued-job redrive, objective-control outbox redrive, cron watchdog) plus startup resume keep work moving.

The biggest surprise since 41baea75 is a projection-durability rewrite spanning ~14 commits: a Postgres trigger now records per-projector/per-stream pending work on every receipt insert, an API-side pump drains it every second, reducer state is checkpointed and refreshed by verified delta pages, projection transactions run on a dedicated single-connection pool, high-volume computer/heartbeat receipts are excluded from objective mirroring, and the Resonate poll transport was replaced so idle workers no longer disconnect. Tool-reliability fixes stopped active-integration inventory from becoming required dependencies, made permanent model 401s non-retryable with a BYOK guidance message, removed per-worker OpenSandbox host auto-stop, and made stale audits complete as "superseded".

Credential handling is precise but must be documented carefully: AWS/GCP/kubectl/Jira CLIs inside the sandbox use generated credential helpers that fetch material from the Receipt Connect gateway at call time under a job-scoped JWT, and generic HTTP integrations are proxied server-side so their secrets never enter the sandbox. However the organization's OpenAI BYOK key is written to ~/.codex/auth.json, and a controller-host gh login is projected as GH_TOKEN plus hosts.yml. Other findings: checks run inside the sandbox (repo docs say local); "single repo slot" is board wording while repoSlotConcurrency defaults to 20 and investigation objectives consume no slot; the profile default mode is investigation with auto-promotion forced off; receipt factory doctor remains inert; docs/factory-on-receipt.md's status list is stale. The report records exact UI strings for /tasks ("Beetle Tasks", "New task" panel, stage labels), phase lines, promotion-gate messages, board section descriptions, and the chat-turn-to-objective and enqueue-to-settle flows.

MARKETING CLAIMS:
- [supported] Durable, crash-safe background execution :: Resonate durable promises plus receipt-queue lease/heartbeat/attempt fences, delivery generations, three recovery loops, and (since 41baea75) a trigger-backed projection outbox that replays rather than loses work (adapters/resonate-runtime.ts:553-806, db/projection-work-schema.ts, docs/projection-durability-fix.md).
- [supported] Hash-chained receipts are the complete audit trail of every run :: Every objective, task, candidate, integration, computer lease/command, credential setup, and job transition is a receipt on factory/objectives/<id> or jobs/<id>; projections are rebuildable from replay (modules/factory/reducer.ts case list, runtime-contracts.ts:19-35).
- [supported] All agent work runs in an isolated sandbox with no unsandboxed path :: FactoryExecutionPath is only "computer" with provider "opensandbox"; bounded cpu/memory/timeouts (types.ts:87-96, opensandbox-sandbox-create.ts:11-37). Note the Codex kernel sandbox itself is bypassed inside the container (lima-remote-codex-args.ts:305-311).
- [supported] Agents use real cloud CLIs (aws, gcloud, kubectl) against connected systems :: Credential helpers and wrappers are generated per connection inside the sandbox (lima-receipt-connect-provider-aws.ts, -gcp.ts, -kubectl.ts); AWS profile region is hard-coded us-east-1.
- [partial] Raw secrets are never injected into the sandbox :: Connected-system credentials are fetched at call time under a job-scoped token and generic integrations are proxied; but the org OpenAI BYOK key is written to ~/.codex/auth.json and a host gh login is projected as GH_TOKEN plus hosts.yml (lima-auth-byok.ts:73-97, lima-auth-gh.ts:15-60).
- [supported] Factory opens a pull request with validated changes :: Delivery objectives flow task worktree -> integration worktree -> sandboxed checks -> promotion gate -> factory.integration.publish via the factory-pr-publisher skill; requires a GitHub token reachable by the controller (skills/factory-pr-publisher/SKILL.md, worker-result-publish.ts).
- [partial] Factory is self-improving :: It is an audit-and-recommend loop: terminal objectives are audited into artifacts and memory; recommendations are applied only by explicit operator action on the private runtime shell, and repo-wide reports are gated by FACTORY_OBJECTIVE_AUDIT_SYSTEM_IMPROVEMENT (factory-runtime.ts:1176-1510, register-factory-api-routes.ts:520-580).
- [supported] A chat message becomes a tracked background task :: The app router enqueues factory.run on lane chat; progress is mirrored back to the thread and the terminal answer is posted (receipt-chat.service.ts:2144-2200, 3757-3775; bootstrap.ts:705-780).
- [partial] One objective at a time holds the repo execution slot :: The board says so, but the runtime admits up to repoSlotConcurrency (default 20) delivery objectives and investigation objectives consume no slot (objective-slot-control-rebalance-admit.ts:46-53, factory-cli/config.ts:62).
- [unsupported] receipt factory doctor reviews a run with an LLM :: Always throws `Organization BYOK key required for factory doctor review` (factory-cli/doctor.ts:449-451).
- [unsupported] Idle computers are stopped automatically :: Per-worker host idle auto-stop was removed in e6944514; host shutdown is an explicit operator action (opensandbox-host-lifecycle.ts:16-20).

OPEN QUESTIONS:
- Is the factory.dispatch worker group meant to be control (RECEIPT_JOB_KIND_CONTRACTS) or chat (resolveWorkerTarget)? The two still disagree at HEAD.
- Will receipt factory doctor be wired to BYOK or removed from the public surface?
- Should public docs describe repoSlotConcurrency (default 20) or keep the board's "the repo execution slot" wording?
- Is projecting the controller host's gh token into every sandbox intended for hosted deployments, or only local operator runs? It is the one credential that is not job-scoped.
- Generated AWS profiles hard-code region us-east-1; is region selection planned, and should docs tell users to pass --region?
- With host auto-stop removed, what is the documented operator procedure for stopping idle OpenSandbox hosts?
- Which of /factory, /factory-new, /factory-preview is the supported runtime shell, or is /tasks the only product surface to document?
- Are model ids gpt-5.6-luna / gpt-5.6-terra public names safe to print in a configuration reference?
- docs/factory-on-receipt.md and docs/factory-agent-orchestration.md are partly stale (status list, local validation); should they be corrected before public docs cite them?
- Is FACTORY_OBJECTIVE_AUDIT_SYSTEM_IMPROVEMENT intended to ship, and does the app UI ever surface self-improvement recommendations?

### v2:5832908c80636757f6fa9ccc50220105ee529f51664ce5be7147fd1b6c9fc9a9 :: research/2026-09-08/A-coworker-chat.md
The Receipt co-worker chat at HEAD c3c16be6 is a router-first pipeline: POST /api/chat → ChatOrchestratorService (rate limit, CAS, context window, quota) → ReceiptChatService, which fetches a signed Receipt Connect capability snapshot and runs a classification-only "Beetle chat router" that returns chat, organization_skill, or factory. The direct-answer path streams from the Beetle profile prompt with tools: {} and activeTools: [] — no provider-native tool is attached, so the composer's Tools checkboxes are inert at runtime. The factory path enqueues one factory.run job (lane 'chat', sessionKey factory-chat:<stream>, maxIterations 1, memoryScope 'factory-chat:auto') whose chat lane issues a single factory.dispatch; the objective supervisor does the rest and the answer lands via onAsyncSettled plus the Zero-synced objective projection reachable at /chat?objective=<id>.

The most important change since 41baea75 is routing fail-closed semantics: an unparseable or provider-incomplete router decision is now retried once and then throws "Receipt couldn't determine the access needed for this request. Please retry; no task was started." Web users see it inside the "Beetle stopped before completing this response." message; Slack posts it directly and records slack.thread.routing_failed; Teams was not updated and still falls back to Factory. Integration inventory is no longer treated as a dependency source. The composer gained a Skills → Create skill submenu (navigates to an admin-only settings page), Cmd/Ctrl+K gained a Skills action, all sidebar tooltips were removed, and the Replay dialog got real tabs, a "Recorded outcome" section, and a 25 s recap timeout.

On the positioning claims: Slack and Teams adapters are real but asymmetric (Teams has no progress, no thread context, and needs OpenAI BYOK to run Factory); the connector catalog lists 63 provider slugs, not 1000+; planning/executing/reporting and receipt-backed proof are supported. "Read-only by default" holds only at the connector policy level (read actions enabled by default, writes need an owner/admin to enable them), while Factory workers automatically receive connect:write whenever a capability is required. "Waits for your explicit approval" is unsupported: there is no approval gate in web chat, Slack, Teams, or Factory; the Policies → Tool approval panel promises one but no code reads tool_approval rules; guardrail enforcement helpers have no callers; Codex runs with approvals bypassed. Model and reasoning pickers remain unmounted; /tasks and /sessions are direct-URL only; Usage/Billing render nowhere in the sidebar; preference memory is not injected into web chat.

MARKETING CLAIMS:
- [partial] Lives in Slack or Teams :: Both adapters exist and run mention → route → Factory → result, but Teams is custom-upload only, has no progress updates/thread context, and needs OpenAI BYOK; Slack install is admin-only.
- [unsupported] connected with 1000+ apps :: catalog.json lists 63 provider slugs at HEAD; docs should derive the number from receiptConnectConnectorCatalog().
- [supported] Give it a goal in plain language, not a workflow :: Semantic router (no keyword matching) plus objective supervisor planning; Replay shows Goal → Plan → Work stages.
- [supported] It plans, executes, and reports back on its own :: Chat lane dispatches one factory.dispatch; supervisor iterates; answer lands via onAsyncSettled/objective projection; Slack/Teams post terminal messages.
- [partial] Works across every system you have connected :: Providers are selected by the router from the signed capability snapshot; since e6944514 the full inventory is explicitly not a dependency source; writes need admin enablement.
- [partial] Read-only by default :: True at connector policy level (read actions enabled by default, writes need owner/admin enablement), but Factory workers receive connect:write whenever any capability is required.
- [unsupported] any action that changes state waits for your explicit approval :: No approval gate in web chat, Slack, Teams, or Factory; Tool approval policy UI is inert; guardrail helpers have no callers; Codex runs with approvals bypassed; only the SDK human action offers this to custom agent authors.
- [supported] Proof, not just output :: Every turn/run is receipt-backed; Agent replay exposes stages, counters, receipt inspector, replay-to-step, and print.

OPEN QUESTIONS:
- Is Teams intended to keep falling back to Factory on routing failure (teams-routing.ts:28-54), or was it missed in ab7b00b0?
- Is Tool approval (policies.ts tool_approval) scheduled for runtime enforcement, and at which boundary (receipt-connect-call per action, objective supervisor, or SDK human action)?
- Are guardPrompt/guardResponse/guardToolResult meant to be wired into ChatOrchestratorService? The service comment says so; no caller exists.
- Should Factory workers get connect:write only for delivery objectives rather than whenever any capability is required (objective-execution-contract.ts:49)?
- Should the composer's Skills → Create skill item be hidden for non-admin members, who are redirected home by the org-settings layout?
- Usage and Billing are railPlacement 'utility' but never rendered; is a utility group planned or should they move to the organization menu?
- Is the assistant publicly named Beetle or Receipt? The Chat area is titled 'Chat' while prose says 'Beetle' and placeholders say 'Ask Receipt to …'.
- Which Teams doc is canonical for env names: docs/ms-teams-testing-setup.md (TEAMS_-prefixed, wrong) or docs/teams-app-private-distribution.md (unprefixed, matches code)?
- Should docs state a connector count at all, given catalog.json moved from 62 to 63 within 43 commits?

### v2:26dc89361e52bb3ccd3cbab3f528c7b9af59fc0828d2a74ecc5c5733d69f5c75 :: research/2026-09-08/D-mcp-gateway-connect.md
The report re-verifies the MCP Gateway and Receipt Connect against HEAD c3c16be6, using the prior corpus only as a map. The catalog has 63 connectors (62 plus the new Azure service-principal connector), 91 static tools (88 read, 3 write, all writes Apollo), 45 OAuth2, 17 provider-only, 1 custom; 18 typed-proxy, 2 provider-MCP, 2 Atlassian resource-aware, 2 command-auth, 39 compatibility-read. The aggregate MCP server at POST /connect/mcp is fully documented: protocol versions, serverInfo receipt-connect/1.0.0, the verbatim instructions string, the receipt_<provider>_<name>_<tool>_<sha256[:20]> alias scheme, publication rules (valid nango-reference rows only, provider-MCP excluded, writes only with connect:write), and every JSON-RPC error shape. All 26 /connect/* routes are tabulated with scope and authority checks.

Two findings materially affect the positioning. First, gateway receipts are written only by POST /connect/call; the /connect/mcp route never calls recordReceiptConnectToolCall, so tools/call from the Codex bridge or receipt tools call (aggregate form) leaves no tool.called/tool.observed receipt and is invisible on the Gateway activity dashboard. Second, the CLI session JWT always carries connect:credential for 12 hours with no revocation (jti is minted but never checked), and POST /connect/credential/<provider> returns the Nango connection's raw credentials object for any connected connector in that workspace; the web token never carries connect:credential. A stolen server .env (JWT secret, Nango secret key, encryption key) is not worthless. The narrow true claim is that MCP client configs, sandboxes, and the tool path contain no provider credentials.

Scope rules: web chat and the Slack app read the hidden Global scope (ws_global_<md5>, name "Global integrations", never listed); CLI/MCP read the token's workspace (Default after login until receipt workspace use); the Teams app reads Default, an inconsistency. Only Codex has an automated MCP install; other stdio clients get a manual generic config; ChatGPT cannot attach because there is no OAuth/remote MCP surface. Changes since 41baea75 include the Azure connector, split execution-scope errors (403 receipt_connect_execution_scope_missing), CLI login/logout/doctor with a baked hosted origin and session expiry checks, the UI pass (View details dialog, shared tab strip, setup-panel step 1 receipt setup), and pending workspace invites. Every user-visible string, page title, and error message is recorded verbatim with path:line anchors. Prose is about 7,500 words plus about 3,800 words inside the enumeration tables.

MARKETING CLAIMS:
- [partial] One tool to find any app, see what it can do, and take action. :: Find: 63 connectable connectors plus an 894-row display-only Nango directory rendered as 'Coming soon'. See: tools/list, receipt tools describe, and Manage tools are supported. Take action: only 3 of 91 static tools are writes (all Apollo); provider-MCP writes are excluded from the aggregate; the rest is GET-only.
- [partial] Because agents never touch real credentials (scoped passes at the gateway) a stolen .env file is worthless. :: True for the MCP/tool path, MCP client configs, and sandbox environments. False as stated: a server .env holds RECEIPT_CONNECT_JWT_SECRET, RECEIPT_INTEGRATIONS_SECRET_KEY, and RECEIPT_CONNECTION_ENCRYPTION_KEY_B64; the CLI session JWT carries connect:credential, can call POST /connect/credential/<provider> to obtain raw provider credentials, lives 12 h, and cannot be revoked.
- [partial] Deploy governed agents across your entire stack (Slack, Claude, Cursor, ChatGPT, Codex, the web). :: Codex: automated install. Web: chat reads Global Integrations. Slack: the Slack app routes mentions via Global Integrations, not MCP. Claude and Cursor: manual generic stdio config only, never named in code. ChatGPT: no OAuth/remote MCP surface, cannot attach. Teams reads a different scope than web and Slack.
- [partial] Approved tools, scoped identity, secure runtime. :: Approved tools (reviewed manifests, opaque aliases, fail-closed allowlist) and scoped identity (three-scope JWT bound to org and workspace, membership re-checked per call) are supported, minus revocation. Secure runtime holds for Factory OpenSandbox workers; an MCP client runs wherever the user runs it and Receipt governs only the gateway hop.
- [unsupported] Every action emits a receipt. :: recordReceiptConnectToolCall is called only by POST /connect/call (receipt-connect-routes.ts:1185-1246); POST /connect/mcp never records, so Codex-bridge and aggregate receipt tools call actions leave no tool.called/tool.observed receipt. Policy and connection changes go to org_activity_log, not receipts. Accurate form: every /connect/call action and every Factory run emits a receipt.

OPEN QUESTIONS:
- Is the absence of receipt writes on POST /connect/mcp a known gap? The dashboard and Activity copy imply gateway calls are recorded, but MCP-bridge calls are not.
- Should the Teams app read Global Integrations like web chat and Slack, or is its Default-workspace read (apps/teams/teams-runtime.ts:11) intentional?
- Is JWT revocation planned? jti is minted but never checked and receipt logout only deletes the local file; docs must describe the 12-hour exposure until then.
- Which MCP clients will be named as supported? Only Codex has an install path; Cursor/Claude Desktop need a manual generic config; ChatGPT needs the deferred remote OAuth surface.
- Should named connections (aws:prod, google-ads:acme) be documented when neither the web UI nor receipt connect <provider> exposes a name field?
- The setup panel omits `receipt workspace use` although device login always binds Default; is the intended story 'set up, then switch', or should login choose the workspace?
- Is 'Beetle Tasks' acceptable in public documentation? It still renders on the Integrations page.
- docs/receipt-integration-surface-audit.md (61 connectors) and docs/receipt-connect-nango.md (Actions editor 'not shown', incomplete env list) are stale; should they be updated before public docs cite them?
- Should public docs describe only the 63 connectable connectors, or also the 894-row browsable directory shown as 'Coming soon' cards?
- The Activity page is unreachable from navigation and self-describes as incomplete; should it be removed or finished before documentation mentions it?
- Working-tree changes not made by this session were observed (docs/LOCAL_SETUP.md moved before the session; kentron-canva-full.png appeared during it); confirm they belong to another process.

### v2:d5a643e71819ea7d450069fd4a6fd4eb2d4900c0a61b4fce2c2bb961ab77df13 :: research/2026-09-08/H1-core-identity-config-deploy.md
The report re-verifies KENTRON CORE identity, tenancy, configuration, local development, and deployment against HEAD c3c16be6, using the 41baea75 corpus only as a map. Identity is one Better Auth instance (auth.service.ts:446) with email+password (8..128 chars), cloud-only email OTP (6 digits, 5 min, 5 attempts, hashed), cloud-only Google OAuth that has no UI button, cloud-only anonymous sessions, multi-session capped at 10, and a TOTP plugin that is registered but has no enrollment UI. Session TTL is not overridden (Better Auth default). Trusted origins are the exact BETTER_AUTH_URL origin plus HTTPS-only RECEIPT_LEGACY_PUBLIC_ORIGINS; secure cookies follow the URL scheme unless BETTER_AUTH_USE_SECURE_COOKIES overrides. The /api/dev/session-login route is gated only by import.meta.env.DEV plus a loopback hostname. Root "/" now renders the landing page only on beetle.run/www.beetle.run and redirects everyone else to /auth/sign-up. Organizations are auto-created at signup ("<Name>'s Workspace", slug + user-id prefix, advisory lock), capped at 10 per user, with a Free/5-seat baseline and a USD 5 signup credit in cloud, or self_hosted/100000 seats in self-hosted mode; the seat ceiling is now overridable via RECEIPT_DEFAULT_FREE_SEAT_COUNT and enforced on invitations with "Only {n} users can access this workspace." Members, invite (batch 10, 48-hour expiry), general settings (name maxLength 64), and user settings strings are recorded verbatim. SSO/domains/SCIM, passkeys, 2FA enrollment, social linking, and a signup-policy UI are stubbed or absent. The environment reference is the centerpiece: every variable read by apps/start (server and VITE_*), packages/receipt-app, apps/slack, apps/teams, start-all.mjs, local-up.sh, the dev script, the role supervisor, and the deploy scripts, with defaults, readers, template-only dead entries, and the irreversible-rotation caveat for both AES-256-GCM keys. Local development covers prerequisites (Node 24, Bun 1.3.12 via ./bunw, Docker, uv/uvx, resonate >= 0.9.7, codex), the three run modes, the full port map with the variable that moves each port, the "set -a; source" rule for local:up, i18n-before-db-reset, web:db:reset versus zero:migrate, validate:stack, the mock LLM proxy, stop semantics (never kill -9), and a 25-row troubleshooting table. Deployment covers the SST multi-service stack, the single-host Caddy/Compose topology, the CodeBuild path, the service gateway and private prefixes, health endpoints (gate on /readyz), the secrets bundle by name, the release-manifest immutability guard, migration ordering, domain/TLS, email/storage/Redis/vector/markdown/Nango/OpenSandbox, and a self-hoster-versus-internal split. Surprises: the Free plan catalog says 1 seat while code enforces 5; the setup wizard still reports auth email as unavailable in self-hosted mode although SMTP invitations now work; updateSelfHostedSignupPolicy has no caller; OTLP export exists only in the web app via EFFECT_* variables; and an uncommitted move of LOCAL_SETUP.md to docs/ appeared in the working tree during research (not made here).

MARKETING CLAIMS:
- [supported] Better Auth for auth, organizations, invitations, and roles (README) :: auth.service.ts:446-793; organization plugin, invitations, default roles verified
- [partial] Stripe + Resend for billing and email flows (README) :: Stripe is cloud-only and needs both STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET; email provider is disabled|resend|ses|smtp and production deploys require SES
- [supported] PostgreSQL + Redis for persistence and stream continuity (README) :: REDIS_URL backs rate limiting and stream resume; VITE_DISABLE_REDIS in-memory fallback exists
- [partial] Qdrant for vector search/RAG workflows (README) :: Behind VITE_ENABLE_EMBEDDING; hosted builds hardcode it false (sst.config.ts:311, 906) and no Qdrant resource is provisioned
- [partial] One-command deploy of the listed services (README) :: README service list is stale (no receipt-zero-cache service; two Zero services, four runtime role services, Teams omitted); guards and a two-pass Nango secret flow are required
- [unsupported] Free plan: single-member workspace / includedSeats 1 (pricing catalog) :: Enforced default is 5 seats (RECEIPT_DEFAULT_FREE_SEAT_COUNT), workspace-usage/shared.ts:27-34
- [unsupported] Scale/Enterprise features: SAML SSO, Verified domains, Directory provisioning (pricing catalog) :: Organization Security page is stubbed; no SSO plugin or directory-sync code
- [supported] Self-hosted: unlimited usage, cloud-only controls disabled :: self_hosted plan with 100000 seats, disabled usage policy, Stripe/anonymous/OTP removed
- [unsupported] Sign in with Google / Continue as guest (strings in en.json) :: No UI button renders them; Google OAuth reachable only by direct endpoint in cloud
- [unsupported] Two-factor authentication (settings strings) :: Plugin registered and sign-in challenge works, but no enrollment UI exists
- [partial] ZDR (Zero Data Retention) compliance at provider level (README) :: An organization compliance flag filtering models by catalog metadata, not an infrastructure control
- [n/a] Audit-friendly agents that improve every run (landing page) :: Product positioning outside this report's scope

OPEN QUESTIONS:
- Exact Better Auth session expiresIn/updateAge in the pinned 1.5.2 build (Receipt sets none).
- Is a UI/CLI for updateSelfHostedSignupPolicy planned, or must operators edit instance_settings directly?
- Should the setup wizard's 'Auth email: not_available_in_self_host' row be corrected now that SMTP invitations work in self-hosted mode?
- Is key rotation for BYOK_ENCRYPTION_KEY_B64 / RECEIPT_CONNECTION_ENCRYPTION_KEY_B64 planned, or should docs say rotation is unsupported?
- Which is the product intent for Free seats: catalog includedSeats 1 or the enforced default 5?
- Is Google OAuth meant to be reachable (button missing) or dormant?
- Will the two-factor enrollment UI ship given plugin, strings, and challenge exist?
- Should deploy/Dockerfile.receipt-zero-cache and Dockerfile.runtime-hotfix be documented, marked internal, or removed?
- How is a new factory-lite stage provisioned the first time, given deploy:aws rejects it?
- Should resonate be installed as 'resonate' (version-checked) or 'resonate-server' (unchecked legacy flags)? The container pins 0.9.5 below the 0.9.7 floor.
- Which dead .env.example entries are future work versus removable?
- Does apps/start/.env need to exist for zero-cache's --env-file flags?
- What is the public security-reporting address (SECURITY.md names none)?
- Is Windows supported, WSL-only, or unsupported?
- Who moved LOCAL_SETUP.md to docs/ in the working tree, and should citations follow the new path once committed?

### v2:b228e0c4b6db076492563774878989a4f9622282595bbf2ab6f0d7be944d2df0 :: research/2026-09-08/E-llm-gateway.md
The "Model Gateway" at HEAD c3c16be6 is an in-process checkpoint inside the web chat backend (ModelPolicyService -> ModelGatewayService -> Vercel AI SDK streamText) plus a parallel credential-funding path in the Factory runtime. It is not a proxy: apps/start/src/routes/api has no completions, /v1, or proxy route, so "route all LLM traffic" only covers Receipt's own chat, title-generation, embedding, and Factory calls. Only OpenAI (platform-funded via OPENAI_API_KEY, or BYOK) and Anthropic (BYOK, chat only) can execute; 50 of the 75 catalog models are gateway-only rows with no client, and the composer model picker is not mounted, so every turn uses openai/gpt-5.6-luna by default. Contrary to .env.example and the prior corpus, no code reads AI_GATEWAY_API_KEY or ANTHROPIC_API_KEY; the platform route is direct OpenAI (RCA-404), and OPENAI_API_KEY is absent from .env.example.

Policy enforcement that is real: provider/model deny lists and the ZDR flag (bypassed when an executable org key is marked configured) and CHAT_REQUIRE_BYOK all fire in resolveThreadModel before reservation and before the provider call, returning 403 with machine reasons. Inert: "Require organization provider key" (never read), the Policies page rate/budget/logging rules (receipts with no consumer), guardrails (service exists, chat never calls it), and plan feature gates (always allowed). "Enforce Study Mode" pins openai/gpt-oss-120b whose providers are ['azure','gateway'], so by code reading every turn in an enforcing org fails with a 502.

Spend control is dollar-denominated and pre-authorized: a $5 signup credit per hosted org, per-seat prorated cycle budgets on paid plans (needs WORKSPACE_USAGE_TARGET_MARGIN_PERCENT), and a Singularity-set org cap, reserved before the model runs and settled afterwards; BYOK bypasses all of it and there is no token budget. The per-user 30/min fixed-window limiter is Postgres-backed but is disabled entirely when VITE_DISABLE_REDIS=true, which .env.example ships. Audit: each turn writes org_usage_event/org_monetization_event rows, a metadata-only wide-event log (mirrored to PostHog on failure), and a response.finalized receipt for direct answers; no prompt/completion bodies are logged and org_ai_policy edits are unversioned. Surprises: the BYOK page writes an org-global scope while web chat resolves keys at the workspace scope, and the global providerKeyStatus snapshot appears to short-circuit workspace-only keys; the Playground route is a bare heading; /model-gateway renders an empty layout; the Usage page is per-user over 31 days, not org-wide. Only two gateway-path commits landed since 41baea75 (free-seat env override; tab component), plus fail-closed router changes and non-retryable 401s in Factory.

MARKETING CLAIMS:
- [partial] Route all LLM traffic through a single, policy-enforcing checkpoint that prevents costly failures and compliance breaches :: A single in-process checkpoint (ModelPolicyService + ModelGatewayService, plus Factory funding) exists, but it covers only Receipt's own chat/Factory/title/embedding calls; there is no proxy or OpenAI-compatible endpoint, and only OpenAI and Anthropic execute (report sections 3.2, 8).
- [partial] Real-time policy enforcement blocks non-compliant requests before they generate costs :: Provider/model deny lists, ZDR, and CHAT_REQUIRE_BYOK fire before reservation and before the provider call with 403s; but 'Require organization provider key', Policies-page rules, and guardrails are inert, and Enforce Study Mode currently breaks every turn (sections 4.1-4.4, 7.4).
- [partial] Unified audit trail tracks every LLM request :: Each turn yields usage/monetization rows, a metadata-only wide event, and a receipt for direct answers; but no prompt/completion bodies are logged, policy edits are unversioned, router sub-calls are folded into the turn, and the only UI is a per-user 31-day summary (sections 7.6-7.7).
- [partial] Intelligent rate limiting and token budgets prevent runaway spending :: Rate limiting is a fixed 60s/30-request window per user and is disabled under the shipped VITE_DISABLE_REDIS=true; dollar budgets (signup credit, seat cycle, admin cap) are pre-authorized but bypassed by BYOK; no token budget exists and nothing is 'intelligent' (sections 3.6, 7.3-7.4).

OPEN QUESTIONS:
- Does a workspace 'LLM keys' OpenAI key ever get used by web chat when the organization has no global key, given the global providerKeyStatus snapshot short-circuit (model-policy.service.ts:319-332; byok-executor.service.ts:163-184)? And does an org-global BYOK key apply to chat in any workspace at all? Needs a runtime test.
- Is 'Enforce Study Mode' known to be broken at HEAD (fixed model openai/gpt-oss-120b has no executable route), or is a gateway adapter expected?
- Is the Anthropic BYOK card intentionally hidden for launch, and should docs mention Anthropic (chat-only, BYOK-only, unsupported by Factory)?
- AI_GATEWAY_API_KEY / ANTHROPIC_API_KEY remain in .env.example while OPENAI_API_KEY is missing from it; will .env.example be corrected and should docs list OPENAI_API_KEY as required for hosted and self-hosted?
- Should the Usage page be documented as per-user (current query) or is an org-wide view planned? The section copy and the query disagree.
- Is the /model-gateway/playground stub scheduled, or should the rail item be hidden before docs ship?
- The platform-credit exhaustion message points at 'Billing & Credits'; which page name will ship?
- Is disabling the rate limiter under VITE_DISABLE_REDIS=true intentional for self-hosted, given the limiter is Postgres-backed?
- Should org_ai_policy edits become receipt-backed to support the audit-trail claim?
- Are the 21 'managed' provider tiles (AWS, Azure, Vertex, Databricks, Snowflake, etc.) expected to gain adapters, or should docs describe the gallery as a roadmap surface?
- Working tree note: git status now shows LOCAL_SETUP.md deleted, docs/LOCAL_SETUP.md and kentron-canva-full.png untracked; these were not produced by this research session.

### v2:8200eefbb97b5a0593af398689b7e5afde8a29d6fcabfab6ae85ff6c635e5be7 :: research/2026-09-08/I-receipt-cli.md
The report documents the public `receipt` binary (entrypoint packages/receipt-app/src/connect-cli.ts) at HEAD c3c16be6, verified by reading every module it imports, its tests, the release and installer scripts, the server device-login route, and by running the shipped v0.1.0-preview.7 darwin-arm64 binary read-only under `env -i`.

The most consequential finding is that v0.1.0-preview.7 is already published: the live installer at kentronai/receipt-cli defaults to that tag and is byte-identical to the dist copy, and the release checksums match. The repo docs (docs/receipt-cli.md:20-28, docs/receipt-cli-debug-report.md:236-240) still call it unpublished. The binary resolves https://app.kentron.ai with no environment (compile-time RECEIPT_CLI_DEFAULT_PROD_GATEWAY_URL), prints `receipt v0.1.0-preview.7`, and `doctor` reports gateway HTTP 200 on a clean HOME.

Since 41baea75 the public binary gained `login`, `logout`, and a read-only `doctor`; `setup` refuses expired JWTs and promotes the chosen target to the active session; `connect status|disconnect|<provider>` reuse the saved session with one automatic re-login on 401; `connect list|tools|call` read the session when no env token is set (env still wins); a single URL normalizer treats localhost and 127.0.0.1 alike; the catalog grew from 62 to 63 connectors (`azure`). A direct consequence not yet reflected in the public pages: CLI-created connections now land in the workspace bound to the saved token, and `connect status` lists that workspace, so connect.mdx and workspaces.mdx statements about "always Default" are stale.

Still-present traps are recorded with exact strings: the numbered onboarding menu fails for multi-word labels (`unsupported Receipt Connect onboarding choice: Google Cloud`); `--help` does not work on tools/mcp/workspace or the agent surface; `receipt --version --json` falls through to a sign-in (observed); `--no-open <word>` swallows the word; `--wait false` is inert; `workspace --target=dev` is ignored; `disconnect` defaults to aws:default; global flags before the command word misroute tools/mcp/workspace. Metadata-mode import redacts only the raw payload; the normalized receipt still carries prompt and tool text and is uploaded by default after sign-in. The session file stores the 12-hour token in plaintext at mode 0600. The installer never installs the Claude proxy companion and preview.7 does not bundle one.

The report also covers the URL-resolution precedence for prod/dev/local/custom, the device-login protocol and approval-page strings, every env var, exit codes and output envelopes, a troubleshooting table, the in-repo developer CLI (launched via .receipt/bin/receipt, with a different `doctor` and a `{ok, command, mode, data, error, artifactRefs, nextCommands}` factory-agent envelope), and per-page corrections to doc/cli/*.mdx and the prior research.

MARKETING CLAIMS:
- [supported] Works out of the box on a fresh machine; nothing to configure for the hosted app (public README) :: Baked origin verified under env -i on the shipped binary; doctor ok; release and installer published.
- [supported] Your saved sessions in ~/.receipt are kept on upgrade (README) :: install.sh only replaces the binary (install.sh:97-98).
- [supported] receipt doctor never opens a browser and never prints your token (README) :: runDoctor makes two GET probes only; tests assert no token in output (connect-cli.test.ts:704-751).
- [supported] The saved token lasts 12 hours; when it expires receipt setup notices and signs you in again (README) :: JWT TTL 12h (receipt-connect-auth-token.ts:24); expiry check in setup (connect-cli.ts:426).
- [partial] Receipt stores only encrypted org-scoped connection references (README) :: True for Nango references per docs/receipt-cli.md:82-84; not verified beyond the CLI here, and the CLI session file itself holds a plaintext token.
- [partial] Shows only non-secret metadata before anything is stored (help item 5) :: connect status prints account id/principal/expiry only, but the printed Nango Open: link carries a session token and observe uploads prompt text in normalized receipts.
- [partial] Installs the local Claude observer in the background when bundled (help item 7) :: Code path exists (connect-cli.ts:498-524) but no release bundles the companion and install.sh would not install it.
- [partial] Lets you connect 63 providers through Nango or supported CLI credential import (help item 4) :: All 63 ids are accepted client-side; oauth2/custom connectors need the deployment's Nango integration configured, which the CLI cannot check.
- [supported] receipt connect lets a signed-in user connect workspace resources through Receipt's hosted Nango-backed Connect flow (README) :: connect <provider> posts to /connect/nango/sessions and polls /connect/connections (connect-cli.ts:670-711).

OPEN QUESTIONS:
- Is a receipt-claude-proxy companion planned for a release, and will install.sh be updated to install it (docs/receipt-cli.md:277-280 describes the step but no installer does it)?
- Should doc/cli/connect.mdx and workspaces.mdx be updated now to say CLI-created connections follow the session's workspace (code says so at HEAD; not exercised against production with a non-Default workspace)?
- Will the numbered onboarding menu be fixed to map labels to ids (connect-cli.ts:784-787), or should docs keep steering users to `receipt connect <id>`?
- Should --version tolerate extra arguments so `receipt --version --json` cannot start a sign-in?
- Is metadata mode intended to upload prompt and tool text in the normalized receipt, given docs/receipt-cli.md:170-171 claims otherwise?
- doctor treats a 3xx from /health as unhealthy because redirects are not followed; intended for deployments that redirect http to https?
- Which codex version is `mcp install` verified against, and should docs pin it?
- Should logout accept --target so a non-active target's session.<target>.json can be removed?
- Server-side workspace error strings (Default workspace cannot be deleted, owner/admin required, name validation) were taken from prior research and not re-read in services/receipt-workspaces.ts at HEAD; confirm before quoting.

### v2:80a6821d69497c5cfc39e3192967b5198ba77f23f60b6a112db62467fb46c8aa :: (no summary) keys=['checked', 'confidence', 'corrections_file', 'corrections', 'missing_topics']