# Fact-check: H1-core-identity-config-deploy.md

Checked against the Receipt monorepo. The report says it was written at `c3c16be6`; the working tree is now at `d004ba19` (two commits later: `c87b2692` #188, `d004ba19` #189). Those two commits touch 22 files, none of which are the auth, feature-flag, billing, supervisor, or deploy sources the report cites, so every claim below was verified against the working tree and, where a cited file changed after `c3c16be6`, against `git show c3c16be6:<path>`. Paths are repo-relative to `<repo>`. Line numbers are from the working tree.

Verdict scale: **confirmed** (source agrees), **wrong** (source contradicts, citation given), **unverifiable** (no evidence either way found).

## 1. The 20 most consequential claims

| # | Claim (report section) | Verdict | Correction | Evidence (path:line) |
|---|---|---|---|---|
| 1 | One `betterAuth(...)` call at `auth.service.ts:446`, `basePath: '/api/auth'` (:449); versions `better-auth ^1.5.2`, `@better-auth/core 1.5.2`, `@better-auth/stripe ^1.5.4`, `@better-auth/cli ^1.4.21` (§1) | confirmed | Declared ranges are exact. Note for the docs: the lockfile resolves `better-auth@1.5.4` while `@better-auth/core` is pinned exactly at `1.5.2` (root `node_modules/@better-auth/core` is 1.5.2, `node_modules/better-auth` is 1.5.4). Open question 1 ("pinned 1.5.2 build") should say 1.5.4. | `apps/start/src/lib/backend/auth/services/auth.service.ts:446,449`; `apps/start/package.json:39-41,61`; `bun.lock:1922`; `node_modules/better-auth/package.json` (`1.5.4`); `node_modules/@better-auth/core/package.json` (`1.5.2`) |
| 2 | No `session:` block, so Better Auth defaults apply (7-day expiry, daily refresh) (§1.1) | confirmed | Open question 1 is answerable now: the installed build sets `expiresIn = 3600*24*7` and `updateAge = 1440*60`. | `auth.service.ts:446-793` (only `databaseHooks.session`, no top-level `session`); `node_modules/better-auth/dist/context/create-context.mjs:143-144` |
| 3 | Self-hosted `before` hook rejects social and email-delivery paths with `FORBIDDEN` and the exact codes/messages quoted (§1.3) | confirmed | Line cite `226-270` is slightly off: helpers are at 231-248, the guard starts at 250, the two throws are at 257-270. | `auth.service.ts:231-270, 790-792` |
| 4 | `/api/dev/session-login` is gated only by `import.meta.env.DEV && hostname in {localhost,127.0.0.1,::1}`, returns `404 {"error":"Not found"}`, 7-day session, `HttpOnly; SameSite=Lax; Path=/`, relative-only `?redirect=` (§1.5) | confirmed | Cited lines drift: gate is `:29-31` (not 30-32) and `:206-208` (not 214-218). Also rejects `//`-prefixed redirects. | `apps/start/src/routes/api/dev/session-login/route.tsx:5-6,29-31,42-48,186,206-208,224` |
| 5 | Root `/` renders the landing page only for `beetle.run` / `www.beetle.run`; every other host and all self-hosted builds redirect to `/auth/sign-up` (§1.6, §9.3) | confirmed | | `apps/start/src/routes/index.tsx:15-30`; `git show c3c16be6 --stat` lists `index.tsx` |
| 6 | No Google button, no guest button; `grep signIn.social` returns nothing; `auth_sign_in_google` / `auth_login_sign_in_google` / `auth_sign_in_guest` exist in `en.json` but are unused (§2.1) | confirmed | | `grep -rn 'signIn\.social' apps/start/src` = 0 hits; `grep -ci google .../sign-in/login-form.tsx` = 0; `apps/start/messages/en.json:11,12,80`; each key has 0 non-paraglide references under `apps/start/src` |
| 7 | Two-factor enrollment is absent from the UI; the only `twoFactor` client call is `verifyTotp` at sign-in (§3.8) | confirmed | | `apps/start/src/components/auth/sign-in/sign-in-page.logic.ts:603` is the sole `authClient.twoFactor.` call; `apps/start/src/components/settings/security/security-page.tsx` has no `mfa`/`twoFactor`/`passkey` match |
| 8 | Security settings render "a `Passkeys` row reading `"0 passkeys registered"`" (§3.8, listed under *Sections rendered*) | **wrong** | The strings exist only in `en.json`; no component references `settings_security_login_methods_passkeys_*`, and `connected-login-methods.tsx` has no passkey markup. Classify passkeys as *stubbed strings only* (which the report's own "Do not claim" list already does), not as a rendered row. | `apps/start/messages/en.json:680-681`; `grep -rn 'login_methods_passkeys' apps/start/src --include='*.ts*'` (excluding paraglide) = 0 hits |
| 9 | Default organization name rule and slug rule (`slugify(name)` max 40 chars + `-` + first 8 chars of user id) (§3.1) | confirmed | | `apps/start/src/lib/backend/auth/domain/default-organization.helpers.ts:15-31`; `apps/start/src/lib/backend/auth/services/default-organization.service.ts:30-36`; `auth.service.ts:134-138` |
| 10 | `RECEIPT_DEFAULT_FREE_SEAT_COUNT` (positive int, default 5) added in `c3c16be6`; self-hosted seats 100000; signup credit 5_000_000_000 nano-USD cloud only; overflow is HTTP 403 `"Only {n} users can access this workspace."` (§3.1, §3.7) | confirmed | | `apps/start/src/lib/backend/billing/services/workspace-usage/shared.ts:19,27-34` (`git log -S` shows `c3c16be6`); `default-organization.service.ts:28,46-61`; `apps/start/src/lib/backend/billing/domain/seat-limit-message.ts:6-8`; `.../integrations/auth-billing-hooks.ts:79-92`; `.../domain/api-errors.ts:14` (`APIError('FORBIDDEN'`) |
| 11 | Plan catalog says Free `includedSeats: 1` at `access-control/index.ts:99` (§3.7, §5) | confirmed (line wrong) | The value is at `:94` (Free block is `:90-96`); `:99` is inside the Plus block. | `apps/start/src/lib/shared/access-control/index.ts:90-96` |
| 12 | Marketing table row: *Plus/Pro/Scale features "SAML SSO", "Verified domains", "Directory provisioning"* -> unsupported (§Documentation implications) | **wrong attribution** (verdict stands) | Neither Plus nor Pro lists those. `Scale` lists `'SAML SSO', 'Verified domains', 'Higher throughput'`; `Enterprise` lists `'Directory provisioning'`. Also worth flagging: the catalog puts SAML SSO on Scale while `ORG_FEATURE_MINIMUM_PLANS.singleSignOn` is `'enterprise'` and `verifiedDomains` is `'pro'`, so the marketing copy and the entitlement table disagree with each other as well as with the (absent) implementation. | `access-control/index.ts:103,112,122,132` (features arrays); `:164-173` (`ORG_FEATURE_MINIMUM_PLANS`) |
| 13 | Settings access: no role -> false; MCP Gateway or Workspaces paths -> any role; else owner/admin (§3.6) | confirmed | `canAccessOrganizationSettingsPath` only calls `isMcpGatewayPath`, but that predicate covers both `ORG_MCP_GATEWAY_HREF` and `ORG_WORKSPACES_HREF`, so the report's reading is right. | `apps/start/src/routes/(app)/_layout/organization/settings/-organization-settings-access.ts:9-17`; `apps/start/src/components/mcp-gateway/mcp-gateway-nav.config.tsx:17-21` |
| 14 | Organization Security page is stubbed: every button disabled, every href `#`, no SSO/SCIM code (§3.6) | confirmed | | `apps/start/src/components/organization/settings/security/security-page.tsx:9-11` (`'#'` constants), `:33,54,75` (`buttonDisabled`); no SSO/SCIM plugin import in `auth.service.ts` |
| 15 | `/api/admin-metrics`: real non-anonymous email-verified session, email ends `@kentron.ai`, optional `ADMIN_EMAIL_ALLOWLIST` re-read per request; 401 `"Sign in to continue."`, 403 `"This account does not have access to the admin console."`; every read logged with viewer email (§3.9) | confirmed | | `apps/start/src/lib/backend/admin/admin-access.server.ts:19,34-52,61-88`; `apps/start/src/routes/api/admin-metrics.tsx:27-45` |
| 16 | Self-hosted signup guard: 401 `SELF_HOSTED_SETUP_REQUIRED`, `open`, `shared_secret` (403 `SELF_HOSTED_SIGNUP_SECRET_INVALID`), `invite_only` (403 `SELF_HOSTED_INVITE_ONLY`, pending + unexpired + case-insensitive), 400 `SELF_HOSTED_SIGNUP_EMAIL_REQUIRED`; row defaults `invite_only` / `public_app_locked=true` (§9.2) | confirmed | | `auth.service.ts:272-333`; `apps/start/src/lib/backend/self-host/instance-settings.service.ts:87-100` (insert defaults), `:323-341` (`lower(email)`, `status='pending'`, `"expiresAt" > now()`) |
| 17 | `updateSelfHostedSignupPolicy` exists (`:281`) but has no caller; no UI/route changes the policy after setup (§9.2, "absent") | confirmed | | `grep -rn updateSelfHostedSignupPolicy apps packages` returns only the definition at `instance-settings.service.ts:281` |
| 18 | Self-hosted invitations without a mail transport become copyable `<origin>/auth/sign-up?invitationId=<id>` links; "Claim freely: self-hosted mode with ... invite-only default signup, SMTP invitations" (§3.4, §9.3, §Documentation implications) | confirmed as literal, **but misleading** | The link is built as stated. However, in self-hosted mode `/auth/sign-up`'s `beforeLoad` unconditionally redirects to `/auth/sign-in` once setup is complete (present since the initial import `b9bd39b9`, not new), the redirect carries no search params, `/auth/sign-in` `validateSearch` accepts only `redirect` (so `invitationId` is dropped), and `login-form.tsx` hides the "Don't have an account?" toggle when `isSelfHosted`. The accept-invitation page also sends unauthenticated org invitees to `/auth/sign-up`. Net: at `c3c16be6` a self-hosted invitee has no UI path to create an account; only the raw `POST /api/auth/sign-up/email` API path (which the guard permits for a pending invitation) works. The report does not flag this, and the "claim freely" line should be qualified until the product fixes it. | `apps/start/src/components/organization/settings/members/invite-members-dialog.logic.ts:147`; `apps/start/src/components/organization/settings/mcp-gateway/workspace-share-invitation.ts:26`; `apps/start/src/routes/auth/sign-up/route.tsx:10-21`; `apps/start/src/routes/auth/sign-in/route.tsx:21-23`; `apps/start/src/components/auth/sign-in/login-form.tsx:351`; `apps/start/src/components/auth/accept-invitation/accept-invitation-page.tsx:46-64`; `git log -S"snapshot.setupComplete ? '/auth/sign-in' : '/setup'"` -> `b9bd39b9 2026-04-16` |
| 19 | Env loader order in `start-all.mjs:962-990`; `loadEnvFile` skips keys already set (`:987`); `local-up.sh` reads only the shell and writes placeholders at `:172-173` (§6.1, §7.4) | confirmed | | `scripts/start-all.mjs:962-975,987`; `scripts/local-up.sh:172-173,234-249,397-403` |
| 20 | Turbo strict env mode; `globalEnv` = exactly `NODE_ENV, VITE_APP_INSTANCE_MODE, VITE_BETTER_AUTH_URL, VITE_SELF_HOST_SOURCE, VITE_ZERO_CACHE_URL, VITE_ENABLE_ORGANIZATION_PROVIDER_KEYS, VITE_DISABLE_REDIS` (§6.1) | confirmed | | `turbo.json:3-12` |
| 21 | Section 8.9 repo-hygiene note: "a personal GitHub repository as the default `RECEIPT_CLI_REPO_URL` in `scripts/install-receipt-cli.sh`" | **wrong** | The default is the organization repository `https://github.com/kentronai/Receipt.git`, not a personal one. Drop this from the hygiene list (the Tailscale `allowedHosts` entry in `apps/start/vite.config.ts:382` is real and is a `*.ts.net` host; it is correctly not printed in the report). | `scripts/install-receipt-cli.sh:4`; `apps/start/vite.config.ts:382` |
| 22 | `scripts/deploy-aws.mjs` accepts only `factory` and `production` (§8.1) | confirmed | `supportedStages` is derived from `stageAccountEnv` keys. | `scripts/deploy-aws.mjs:12-22,406-414` |
| 23 | Health endpoints: web `GET`/`HEAD /health` -> `{"ok":true}`; runtime `/healthz` always 200 with the listed fields; `/readyz` 200 or 503 (§8.3) | confirmed | | `apps/start/src/routes/health/route.tsx:6-19`; `packages/receipt-app/src/server/bootstrap.ts:2710-2737` |
| 24 | `sst.config.ts`: app `receipt-factory`, default stage `factory`, protected stages, `RECEIPT_ALLOW_PRODUCTION_REMOVE`; RDS Postgres 17 with `rds.logical_replication`; `ZERO_IMAGE` default `rocicorp/zero:1.5.0`; `VITE_DISABLE_EMAIL_VERIFICATION_OTP` defaults `"true"` (§1.4, §8.1) | confirmed | | `sst.config.ts:9-35,102,112-117,224,421,537,586,904-906,917` |
| 25 | Single-host: `RECEIPT_FACTORY_LITE_EIP_ALLOCATION_ID` required (`:30-33`), env stored as one SSM SecureString (`:162`), `zero:migrate` at bootstrap (`:324`), polls `http://127.0.0.1:3000/health` (`:330`), `userDataReplaceOnChange: true` (`:349`), compose services incl. `redis:7-alpine`, `caddy:2-alpine`, gateway bound to `127.0.0.1:3000` (§8.1) | confirmed | | `deploy/sst/single-host.ts:29-34,160-162,324,330,349,494-670,495,652,662` |
| 26 | Release-manifest immutability guard rejects `:latest` / `sst-asset:` with the quoted message (`:67-68`); production starts need `--release-tag` or `--release-manifest-s3-uri`; unpushed/detached HEAD refused unless `RECEIPT_ALLOW_UNPUSHED_DEPLOY=1` (§8.5) | confirmed | | `scripts/beetle-release-manifest.mjs:67-68`; `scripts/start-beetle-deploy.mjs:66-79,279-298` |
| 27 | Secrets bundle required/optional names and loader validation (32-byte keys, `IntegrationSecretKey` UUID v4) (§8.4) | confirmed | | `deploy/factory.secrets.env.example:4-45`; `scripts/load-factory-secrets.mjs:15-45,166-177` |
| 28 | README "One-command deploy" service list is stale: lists `receipt-zero-cache`, omits Teams; SST actually has `ZeroReplicationManager` + `ZeroViewSyncer`, four runtime-role services, `Teams` (§Marketing claims) | confirmed | | `README.md:66-78`; `sst.config.ts:537,586` (two Zero services), `:745,788` (`RECEIPT_PROCESS_ROLE` per role service) |
| 29 | Invitation expiry is Better Auth's 48-hour default (`adapter.mjs:547`) (§2.3) | confirmed | | `node_modules/better-auth/dist/plugins/organization/adapter.mjs:547` (`options?.invitationExpiresIn \|\| 3600 * 48`) |
| 30 | `AUTH_DEV_EMAIL_OTP_TO_CONSOLE`, `RECEIPT_POSTGRES_MIRROR_URL`, `SLACK_PRIMARY_*`, `SLACK_ALLOWED_TEAM_IDS`, `VITE_STRIPE_PUBLISHABLE_KEY`, `STRIPE_PRODUCT_ENTERPRISE` are read by no code; `AI_GATEWAY_API_KEY` only deleted from sandbox env; `VITE_ENABLE_ORGANIZATION_PROVIDER_KEYS` read by no application code (§6.12) | confirmed | `SLACK_BOT_TOKEN` is passed through by `sst.config.ts` and `deploy/sst/single-host.ts` (deploy configs) and read by no app code, which matches the report's wording. | `grep -rln` over `apps packages scripts deploy sst.config.ts turbo.json .github` (tests/examples/md excluded): all NONE except `AI_GATEWAY_API_KEY` -> `packages/receipt-app/src/services/factory/lima-auth-byok.ts:75` (`delete env.AI_GATEWAY_API_KEY`), `VITE_ENABLE_ORGANIZATION_PROVIDER_KEYS` -> build/CI/deploy files only; `apps/start/.env.example:47-48,65`; `apps/start/.env.self-host.example:38` |

(Thirty rows are listed because several of the "20" naturally split into independently checkable statements; the first 20 rows are the most consequential.)

## 2. Additional spot checks (all confirmed unless noted)

- Email/password limits `8..128`, `revokeSessionsOnPasswordReset`, `requireEmailVerification` condition: `auth.service.ts:521-528`.
- Email OTP `otpLength 6`, `expiresIn 5*60`, `allowedAttempts 5`, `storeOTP 'hashed'`, cloud-only: `auth.service.ts:540-563`.
- `organizationLimit: 10`, `membershipLimit` -> `getOrganizationSeatLimit`, `requireEmailVerificationOnInvitation`: `auth.service.ts:565-576`; `auth-billing-hooks.ts:64-73`.
- `multiSession maximumSessions: 10`; `twoFactor` issuer `Receipt`, 6 digits/30 s, 10 backup codes of length 10 encrypted, 10-min cookie, 30-day trust: `auth.service.ts:758-778`.
- Trusted origins HTTPS-only legacy entries, exact-origin canonical URL, error prefixes: `apps/start/src/lib/backend/auth/services/auth-trusted-origins.ts` (whole file).
- `BETTER_AUTH_URL` / `BETTER_AUTH_SECRET` error strings, `BETTER_AUTH_USE_SECURE_COOKIES` override and comment: `auth.service.ts:94-125`.
- `VITE_APP_INSTANCE_MODE`, OTP-disable logic, `ALLOW_USER_COST_DISPLAY`, `VITE_DISABLE_REDIS`, `VITE_CHAT_OBJECTIVE_PUSH`: `apps/start/src/utils/app-feature-flags.ts:25-67`.
- Stripe plugin gated on both keys and cloud; `authorizeReference` uses `isAdminRole` for list and mutate; `allow_promotion_codes: true`: `auth.service.ts:141-219`; `apps/start/src/lib/shared/auth/roles.ts:5-10`.
- `useCanManageOrganizationSettings` fails closed: `apps/start/src/lib/frontend/auth/use-auth.ts:77-108`.
- `/settings` redirects signed-out/anonymous to `/chat`, `"Loading settings"`: `apps/start/src/routes/(app)/_layout/settings/route.tsx:9-17`.
- Sidebar `MAX_ORGANIZATIONS_PER_USER = 10`, `organization.create` then `setActive`, sign-out entry, active-workspace label: `sidebar-organization-menu.tsx:99,302-305,325,341,496-499,607`.
- Members page hardcoded strings and `SELECTABLE_ROLES` (admin/member), invite batch `INVITE_BATCH_MAX = 10`, one `inviteMember` call per address, `"Invitation created. Copy the signup link below."`: `members-page.tsx:75-78,244-246,330-344`; `invite-members-dialog.tsx:75,106`; `invite-members-dialog.logic.ts:9,123-124,147,170`.
- Org name `maxLength: 64`; `organization.update` for both name and logo: `org-general-page.tsx:66`; `org-general-page.logic.ts:90,122`.
- Workspaces page description string present at `c3c16be6` and HEAD: `workspaces-page.tsx:251`.
- `receiptWorkspaceEffectiveRole` maps org owner/admin to workspace owner/admin; Default workspace `ws_<md5(orgId)>` undeletable; global row `ws_global_<md5>`: `packages/receipt-app/src/services/receipt-workspaces.ts:112,149,165,454,588-594`.
- `/pricing` search schema (`plus|pro|scale`, seats 1..500, `resumeCheckout: '1'`) and self-hosted card: `apps/start/src/routes/pricing/route.tsx:11-38`; `self_hosted` plan id valid only when `isSelfHosted`: `access-control/index.ts:212-218` (report cites `:225-229`, drift).
- Setup wizard strings, token query keys `setupToken|setup-token|token`, `/setup` redirects, `x-receipt-setup-token` header, `timingSafeEqual`: `en.json:26-54`; `setup-page.logic.ts:58`; `routes/setup/route.tsx:6-22`; `instance.server.ts:170-243`; `instance-settings.service.ts:214-231`.
- Setup readiness checklist (required/optional rows, `not_available_in_self_host` for Auth email): `instance.server.ts:36-105`. The report's note that the `Auth email` row is misleading is supported by `auth.service.ts:692-695`.
- `publicAppLocked` gate on `(app)` routes: `routes/(app)/_layout/route.tsx:5-28`.
- Slack claim regex `^[A-Za-z0-9_-]{43}$`, Teams `min(32).max(4096)`, sign-in redirect shapes, page strings (hardcoded in the tsx files, not `en.json`): `routes/auth/slack-install/route.tsx:6-25`; `routes/auth/teams-install/route.tsx:6-20`; `slack-install-page.tsx:36-45,102,135,160,231,243`; `teams-install-page.tsx:17,30-31,38,47,51,54`.
- Ports: `start-all.mjs:63,94-99` (gateway/web 3000, internal 3001, zero 4848, receipt 8787, slack 3010, teams 3011, redis 6380, nango 3003/3009, resonate 8001/9090, opensandbox 8080), `docker-compose.local.yml:5` (Redis compose default 6379), `docker-compose.postgres.yml:21`; `dev-with-receipt.mjs:48-59,61-77,80-83`; mock proxy banner `scripts/mock-llm-proxy.mjs:517-518`; loopback-only mock key `packages/receipt-app/src/services/receipt-openai-env.ts:52-74`.
- `start:all` preflight (`npm`, `curl`, `docker`, `uvx`), port assertions on `127.0.0.1` and `::1`, detached children, cleanup handlers, `RECEIPT_POSTGRES_SCHEMA` pin, Connect key fallback: `start-all.mjs:120-141,662,771-776,1020-1024,1082-1090`.
- BYOK and Connect key modules: `packages/receipt-app/src/services/byok-crypto.ts:6,39-51,78-80`; `packages/receipt-app/src/services/receipt-connect-config.ts:114-126`.
- Connect JWT 12 h default, secret fallback and error: `packages/receipt-app/src/services/receipt-connect-auth-token.ts:24,133-134,150`; device-login constants: `apps/start/src/routes/api/receipt-connect/cli-login/route.tsx:12-21`; env identity wins over saved session: `receipt-connect-agent-cli.ts:176-190`.
- CLI release bakes `https://app.kentron.ai`; `login`/`logout`/`doctor` verbs: `scripts/build-receipt-cli-release.sh:7-9`; `packages/receipt-app/src/connect-cli.ts:1132-1147`; `docs/receipt-cli.md:24,39` (`v0.1.0-preview.7`).
- `receipt doctor`: `bun`, `git`, `codex`, and repo validity are blocking; `gh`/`aws` are not in the blocking array: `packages/receipt-app/src/cli/doctor.ts:381-386`.
- `zero_data` publication is a curated list; four `receipt_`-prefixed app-owned tables; `ZERO_PUBLICATION_EXTRA_TABLES`: `apps/start/scripts/zero-publication.ts:5-51`. `db-reset.ts:47,64,73-75,103`; `zero-migrate.ts:1035-1036,1150-1151,1173`.
- `zero-upstream-pool.ts:21-25` sslmode rewrite to `verify-full` unless `uselibpqcompat=true`.
- Preflight/bunw messages: `scripts/preflight.mjs:35`; `scripts/bun.mjs:136`; `.node-version` = `24`.
- Dockerfiles: `deploy/Dockerfile.receipt:3,5,46-49` (Codex `0.130.0`, Resonate `0.9.5` installed as `resonate-server`); `deploy/Dockerfile.nango:1,132-141` (`hosted-0.69.48`, Azure service-principal patch, `EXPOSE 3003 3009`).
- OpenSandbox defaults and AWS pin: `packages/receipt-app/src/services/factory/opensandbox-config-default-values.ts:1-12`; `deploy/sst/config.ts:14-15,56-57,77-83`; `sst.config.ts:1309` (`opensandbox-server==0.2.2`).
- One-off Fargate `zero:migrate` task in `deploy-aws.mjs:120-162`; SES production guard messages `deploy-aws.mjs:653-735`.
- Counts: `docs/agent-fix-checklist.md` = 21,286 lines, highest `RCA-523`; `git rev-list --count 41baea75..c3c16be6` = 43; `sst.config.ts` = 1,491 lines; `deploy/sst/single-host.ts` = 674 lines; `LOCAL_SETUP.md` at `c3c16be6` = 1,275 lines (added 2026-09-06 in `48cdeca8`, last touched 2026-09-07 in `a6a32fc7`; its own text dates the executed run 2026-09-05).
- Commits named in "Changes since 41baea75" (`46652e84`, `9b8b7574`, `3c52d87c`, `53f74bed`, `a6a32fc7`, `48cdeca8`) all exist with matching subjects.
- CI builds `VITE_APP_INSTANCE_MODE: self_hosted` / `VITE_SELF_HOST_SOURCE: render`: `.github/workflows/ci.yml:22-23`. `deploy-factory.yml:3-11,24-43` as described.
- Observability: `posthog-config.server.ts:15-27`; `server-observability.layer.ts:5-7,44-52,85-89`; no `OTEL_` reads anywhere in `apps packages scripts deploy`.
- ZDR marketing verdict "partial": there is an organization-level `zeroDataRetention: boolean` policy flag in `apps/start/src/components/organization/settings/model-policy/types.ts:24` consumed by the chat orchestrator, consistent with "a compliance flag that filters models", not an infrastructure control. Depth not re-verified beyond that (unverifiable at the level of the prior corpus §7.7).
- `.env.self-host.example` is referenced by no `*.md` in the repo (grep = 0 hits, node_modules excluded).

Unverifiable (no evidence found either way, left as the report states): "`RECEIPT_POSTGRES_URL` (105 reads)"; "15 import sites" for session resolution; the `VALIDATE_STACK_*` help block is at `scripts/validate-stack.sh:2336-2375` (confirmed) but individual option semantics were not exercised.

## 3. Cited paths that do not exist

All 167 distinct file citations resolve, with these notes:

- `LOCAL_SETUP.md` (root): exists at `c3c16be6` (`git ls-tree c3c16be6 LOCAL_SETUP.md`), and the working tree shows an uncommitted move to `docs/LOCAL_SETUP.md` (`git status`: ` D LOCAL_SETUP.md`, `?? docs/LOCAL_SETUP.md`). The report's working-copy note is accurate; docs should cite `docs/LOCAL_SETUP.md` if that move lands.
- Short-form citations (`logic.ts`, `route.tsx`, `server/config.ts`, `access-control/index.ts`, `workspace-usage/shared.ts`, `has-permission.mjs`, `.env.example`) are all resolvable from context to `apps/start/src/components/organization/settings/members/invite-members-dialog.logic.ts`, the route named in the same sentence, `packages/receipt-app/src/server/config.ts`, `apps/start/src/lib/shared/access-control/index.ts`, `apps/start/src/lib/backend/billing/services/workspace-usage/shared.ts`, `node_modules/better-auth/dist/plugins/organization/has-permission.mjs`, and `apps/start/.env.example`. `apps/start/node_modules/better-auth` does not exist; the package is hoisted to the root `node_modules`, which is where the report's `node_modules/...` citations point.
- No cited path is missing.

## 4. Leak check

Scanned the report for 12-digit account ids, `arn:`, non-loopback IPv4 literals, `/Users/`, `/home/`, `~/`, credential-shaped strings (`sk-`, `AKIA`, `password=`, `secret=`, `token=`), email addresses, `*.ts.net` / Tailscale hostnames, `github.com/...` URLs, and `org_`/`ws_`/`user_` identifiers.

- **No leaks.** No account ids, ARNs, IPs, home paths, personal hostnames, GitHub handles, org ids, or secrets appear.
- Emails present are examples only (`ari.say@x.com`, `colleague@example.com`, `owner@example.com`).
- Domains present are public product domains (`app.kentron.ai`, `beetle.run`, `www.beetle.run`, `@kentron.ai` as the admin-console email domain).
- The report correctly describes but does not print: the `SINGULARITY_ORG_ID` value, the AWS account id fallback in `scripts/single-host-rollout.mjs:9`, the AWS profile names in `package.json:90-91`, and the Tailscale host in `apps/start/vite.config.ts:382`. Doc writers must keep it that way.
- One item in the hygiene list is mis-described (row 21 above): the `RECEIPT_CLI_REPO_URL` default is the `kentronai/Receipt` organization repository, not a personal one.

## 5. Missing or thin topics in the assigned scope

1. **Self-hosted invitee sign-up is unreachable through the UI** (row 18). The report documents the copyable link and the `invite_only` policy without noticing that `/auth/sign-up` always redirects in self-hosted mode. Docs for self-hosting must either describe the API path or wait for a fix; it should also be raised as an open question.
2. **Self-hosted password recovery.** Password reset is blocked by `selfHostedAuthGuard` (`/request-password-reset`, `/reset-password*`), `changeEmail` is disabled, and no admin-driven reset exists. The report lists the block but does not say how a self-hosted user recovers a lost password (answer at HEAD: they cannot through the product).
3. **Session cookie names and the `__Secure-` prefix.** Section 1.2 covers the `Secure` flag but not the cookie names an operator sees in production versus dev (the dev route hardcodes `better-auth.session_token`; Better Auth prefixes cookies when `useSecureCookies` is true). Needed for the gateway/CDN and load-balancer pages.
4. **Better Auth rate limiting on `/api/auth/*`.** Not mentioned; Receipt sets no `rateLimit` option, so the library default applies. Worth one sentence in the environment reference.
5. **Account deletion / data export.** Neither Better Auth `deleteUser` nor any Receipt-side deletion flow is discussed; a public docs site will be asked.
6. **Version drift of Better Auth.** Declared `^1.5.2` resolves to 1.5.4 while `@better-auth/core` is pinned at 1.5.2 (row 1). Docs should quote the resolved version.
7. **Supported locales.** Section 3.8 mentions the Language setting and `preferredLocale` but not which locales `project.inlang` ships; the docs will need the list.
8. **Backup and restore.** The `ZeroBackups` bucket, RDS snapshots, EFS volumes, and the local `receipt_postgres_data` volume are named, but there is no restore procedure or RPO statement for either topology.
9. **Full-stack rollback.** Only the single-host `single-host:rollout` path is described; how a bad `production` SST deploy is rolled back (re-promoting a previous release manifest) is not covered.
10. **Post-`c3c16be6` invitation-email changes.** `c87b2692` (#188, "stop implying unsent invite mail") added `docs/integrations-and-invite-email-troubleshooting.md` and changed `workspace-share-dialog.tsx`; the invitation/email pages should be re-checked against that commit before publication.
11. **`receipt doctor` warning semantics.** The report says `gh`/`aws` are "warnings only"; the blocking array confirms they are not blocking, but the exact warning text and what `doctor --json` returns for them are not documented.
12. **Windows.** Raised only as open question 14; the prerequisites page needs a definite statement (the supervisor's process-group cleanup is POSIX-only, `local:up` is bash).
