# J1 — adversarial fact-check corrections

**Target:** `J1-existing-docs-audit.md`
**Doc repo:** `~/Desktop/Development/kentron/doc`, audited commit `ed576e9`. Working tree is mid-restructure (`M README.md docs.json introduction.mdx scripts/check-docs.mjs`, `?? co-worker/ mcp-gateway/ images/ research/2026-09-08/`), so every doc-side claim below was checked against a clean `git archive ed576e9` export, not the working tree.
**Implementation repo:** `~/Desktop/Development/kentron/Receipt`. J1 cites `c3c16be6`; **main has since advanced two commits to `d004ba19`** (`c87b2692` "Fix the autonomy filter, surface Skills, and stop implying unsent invite mail", `d004ba19` "Skills chip"). Claims were verified at `c3c16be6` as cited, with drift at `d004ba19` noted.

Verdict key: **confirmed** · **wrong** (with `path:line`) · **unverifiable**.

## Claim table

| # | Claim (J1 location) | Verdict | Evidence |
|---|---|---|---|
| 1 | §1.1 — 49 `.mdx` files: `introduction.mdx` + 48 in six directories (`getting-started/` 5, `guides/` 6, `platform/` 12, `cli/` 8, `develop/` 5, `self-hosting/` 5, `repo/` 7); README's "48" is the undercount | **confirmed** | Directory counts reproduce exactly: 5+6+12+8+5+5+7 = 48, plus root `introduction.mdx` = 49. (The working tree now holds 66 `.mdx` because `co-worker/` has 13 and `mcp-gateway/` 4 new files — those are the restructure, not an error in J1.) |
| 2 | §1.1 — `docs.json` declares six tabs, 18 groups, 49 page slugs; navbar = one **Contact Us** `mailto:feedback@kentron.ai`; footer socials LinkedIn + GitHub; theme `mint`, name `Kentron`, colours `#0D9373`/`#07C983`/`#0D9373`; twelve redirects | **confirmed (content)** | Parsed at `ed576e9`: `tabs` 6, groups 18, page slugs 49, redirects 12; `navbar.links[0]` = `Contact Us` → `mailto:feedback@kentron.ai`; `footer.socials` = linkedin + github; theme/name/colours match. |
| 3 | §1.1 — the line citations `docs.json:16-159` (navigation), `:161-166` (navbar), `:168-173` (footer), `:174-223` (redirects) | **wrong** — `docs.json` at `ed576e9` is 253 lines: navigation `16-188`, navbar `189-196`, footer `197-202`, redirects `203-252` | Every J1 range is ~29 lines short. Checked all three doc commits: `fd54b16` and `15d73d6` put navbar/footer/redirects at `61`/`62`/`63` in a 77-line file, so J1's numbers match **no** committed version — they appear to come from a partially restructured working-tree copy. Fix the four ranges before anyone follows them. |
| 4 | §1.1 — 64,301 words of body text across 49 pages; median ≈1,150; smallest `getting-started/receipts-and-replay` 291; largest `repo/local-development` 4,435 | **confirmed (within method tolerance)** | Re-measured at `ed576e9` with frontmatter, fenced code and JSX tags stripped: total **64,077** (−0.35%), median **1,169**, smallest `receipts-and-replay` **291** (exact), largest `repo/local-development` **4,419** (−0.4%). `introduction.mdx` = **317** exactly, matching J1's per-page cell. Differences are tokenizer-level, not substantive. |
| 5 | §1.3 — per-tab word totals: Guides 6,854 · Platform 17,159 · CLI 8,213 · Developers 5,976 · Self-hosting 8,157 · Working from source 17,942 | **confirmed (within tolerance)** | Measured: 6,852 · 17,141 · 8,186 · 5,927 · 8,150 · 17,821. Largest deviation 0.7% (`repo/`). Page counts per tab (12/12/8/5/5/7) are exact. |
| 6 | §1.2 — `node scripts/check-docs.mjs --complete` fails at HEAD with exactly two problems, both `safety: cli/doctor.mdx contains a personal home directory path ("/Users/you")` | **confirmed** | Ran the validator against a clean `ed576e9` export. Output is verbatim: `2 problem(s)` with those two lines, in both `--complete` and construction mode. |
| 7 | §1.2 / §2.3 — the offenders are `cli/doctor.mdx:88` and `:109` (`"sessionFile": "~/.receipt/session.json"`); `check-docs.mjs:120` permits only `/Users/me`; `cli/setup.mdx` already uses `~/.receipt/session.json` | **confirmed** | `grep -n '/Users/'` on `cli/doctor.mdx` returns exactly lines 88 and 109, in both `ed576e9` and the working tree (still unfixed). `check-docs.mjs:120` = `[/\/Users\/(?!me\b)[A-Za-z0-9._-]+/, 'a personal home directory path']`. `cli/setup.mdx:120` reads "The active session is `~/.receipt/session.json`". |
| 8 | §4 — site-wide component counts: `Warning` 73, `Note` 59, `ParamField` 50, `Step` 45 in `Steps` 11, `ResponseField` 31, `Accordion` 29 in `AccordionGroup` 8, `Info` 14, `Card` 6 in `CardGroup` 2, `Tip` 3, `Check` 1, `CodeGroup` 1, `Expandable` 1; `Frame`/`Tabs`/`Icon`/`Tooltip`/`Columns`/`Snippet` never used | **confirmed — every number exact** | Counted `<Component` opens across all 49 pages at `ed576e9`, with and without code fences stripped (identical for these components): Warning **73**, Note **59**, ParamField **50**, Step **45**, Steps **11**, ResponseField **31**, Accordion **29**, AccordionGroup **8**, Info **14**, Card **6**, CardGroup **2**, Tip **3**, Check **1**, CodeGroup **1**, Expandable **1**. None of the six "never used" components appear. J1's note about excluding TS-generic false positives is also right — `<T>`, `<View>`, `<MergeCandidate>` etc. only appear inside fences. |
| 9 | §7.1 — validator structure: `collect()` key list at `:27`; SKIP set (with `images` at `:43`) and orphan walk `39-67`; frontmatter `69-78`; internal links `80-98` (asset rule `:93`); redirects `100-108` (collision `:103`, destination `:105-107`); publication safety `110-134` with FORBIDDEN entries, marketing domain at `:123`, two named identifiers at `:124` | **confirmed — all line numbers exact** | `scripts/check-docs.mjs` at `ed576e9`: `:27` is the `['tabs','anchors','products','menu','groups','pages']` key list; SKIP declared `40-44` with `'logo','public','images','snippets'` on **`:43`**; orphan/missing block `60-67` inside the `39-67` section; frontmatter loop `70-78`; link loop `83-98` with the asset skip on **`:93`**; redirect loop `101-108`; `FORBIDDEN` array `112-125` inside the `110-134` safety section; `:123` = `/\bbeetle\.run\b/`; `:124` = `/\b(Satish\|knsre)\b/`. |
| 10 | §3.A — `guides/getting-help.mdx:29` says Agent Registry "not active"; it is a working page (title **Agent Registry**, description, tabs Inventory/Dashboard, L1–L4 autonomy filter, **Refresh inventory**) per `agent-registry-page.tsx:50-55,76-77,209-210,222,230,292,369-370` | **confirmed** | Doc line 29 is verbatim. Source at `c3c16be6`: `:50-55` `AUTONOMY_LEVELS = ['L1','L2','L3','L4']`; `:75-78` `TABS` Inventory/Dashboard; `:209-210` title + description exactly as quoted; `:222` `placeholder="Search agents"`; `:230` `Autonomy`; `:292` `Refresh inventory`; `:369-370` the empty states. **Caveat:** commit `c87b2692` (post-`c3c16be6`) says the autonomy filter "never opened, so the L1-L4 levels were unreachable" — so at `c3c16be6` the filter existed in markup but did not function. J1 describes it as working; that is true only from `c87b2692` onward. |
| 11 | §3.A — `getting-started/your-first-chat.mdx:14-19` omits the new `+` → **Skills** → **Create skill** submenu (`prompt-input-actions-menu.tsx:91-107`) | **confirmed at `c3c16be6`; partially superseded at `d004ba19`** | Doc lines 14-19 list Attach files / Study Mode / Max / Tools with no Skills entry. `prompt-input-actions-menu.tsx:91-107` is exactly the `DropdownMenuSub` with trigger `Skills` (`:94`) and item `Create skill` (`:104`) navigating to `/organization/settings/skills` (`:100`). **Drift:** at `d004ba19` the submenu still exists at the same lines, but Skills is *also* now a first-class composer-toolbar chip (`prompt-input-skills-menu.tsx`, new file) that opens a pickable panel and passes `?add=skill` to the Skills page. The rewrite must describe the chip, not only the `+` submenu. |
| 12 | §3.A — `platform/mcp-gateway.mdx:70-78` says five setup commands; there are six, first `receipt setup` (`mcp-gateway-setup-panel.tsx:27-49`) | **confirmed** | Doc line 70 reads "The panel gives five numbered commands"; the fence at 72-78 lists exactly the five. Source `:27-28` = label `Sign in and set up the Receipt CLI` / command `receipt setup`, then `mcp install codex`, `mcp config codex`, `mcp status codex`, `workspace current`, `tools list` ending at `:49`. Six total. |
| 13 | §3.A — `platform/mcp-gateway.mdx:84` says three tabs; there are four including **Dashboard** (`mcp-gateway-workspace-page.tsx:37-41,76-87`) | **confirmed** | Doc line 84 is verbatim "Three tiles sit above three tabs, and each tile links to its tab". Source `:37-41` = `WORKSPACE_TABS` (Open integrations, LLM keys, Settings); `:76-87` = a fourth `<Tab>` linking to `WORKSPACE_DASHBOARD_ROUTE` labelled `Dashboard`. |
| 14 | §3.A — `platform/organizations-and-workspaces.mdx:89` still lists a **Created** column; it is gone and the row menu opens with **View details** (`mcp-gateway-workspace-table.tsx:58-62,104-117,257-296`) | **confirmed** | Doc line 89 is verbatim. Source header row `:104-117` = `#`, `Workspace`, `Sharing and tools`, `Updated`, `Actions` — no `Created`. `:58-62` is the comment explaining why creation facts are not columns ("it lives in View details"). `:257` is the `View details` menu item, ahead of Rename/Share/Delete. |
| 15 | §3.A — `platform/organizations-and-workspaces.mdx:112` describes typing the workspace name; the dialog now requires the literal word `Delete` (`workspaces-page.tsx:197-198,366,383-384,440`) | **confirmed — all five line citations exact** | Doc line 112 is verbatim. Source `:197` `trimmedName !== 'Delete'`; `:198` `'Type Delete exactly to confirm this deletion.'`; `:366` `This is permanent. Type Delete below to delete ${name} …`; `:383-384` label `Type Delete to confirm`; `:440` the disabled guard `name.trim() !== 'Delete'`. (The button label **Delete workspace permanently** is at `:452`, uncited but present.) |
| 16 | §3.A / §2.5 — `self-hosting/database-and-migrations.mdx:72` says "49 timestamped migrations"; the true number is **50** | **confirmed** | Doc line 72 is verbatim. `apps/start/zero/migrations/` at `c3c16be6` holds 51 files: **50** matching `^\d+_.+\.sql$` plus `schema.sql`. Unchanged at `d004ba19`. (Note: J2 states "51 at HEAD" for the same directory — that is the file count including `schema.sql`, and is wrong as a migration count. J1 is the correct one.) |
| 17 | §3.A — `repo/in-repo-cli.mdx:6` ("The released binary also has no `login` command") and `:113` (login/logout/doctor listed as in-repo-only) contradict the released binary (`connect-cli.ts:72-74,1136-1146`) | **confirmed — both doc lines and both source citations exact** | Doc `:6` and `:113` are verbatim as quoted. `connect-cli.ts:72-74` in `printUsage` = `receipt login [prod\|dev\|local\|https://…]`, `receipt logout`, `receipt doctor [--json]`. `:1136-1146` = the dispatch for `login`, `logout`, `doctor\|check`. J1's added detail that `login` takes an optional positional target (`:72`) and that the dispatch order is `--version` → tools → mcp → workspace → connect-agent → help (`:1209-1250`) also reproduces exactly (`:1211`, `:1215-1225`, `:1228-1232`). |
| 18 | §3.B — `introduction.mdx:32` "**Beetle Chat**" was wrong when written; the chat area is titled **Chat** (`en.json:299`, `chat-sidebar.tsx:66,160-162`) | **confirmed — and this is where J2 disagrees and is wrong** | `en.json:299` = `"chat_sidebar_title": "Chat"`. `chat-sidebar.tsx:66` = `CHAT_SIDEBAR_TITLE = () => m.chat_sidebar_title()`; `:160-162` is a **code comment** ("The 88px rail clips \"Beetle Chat\"…") above `railLabel: 'Chat'`. A repo-wide grep for `Beetle Chat` in non-markdown returns only that comment and one test fixture (`app-sidebar-nav.config.test.ts:155`). J1 is right; J2 §4 lists `chat-sidebar.tsx:160` as a shipped UI string, which it is not. |
| 19 | §3.B / §6.1 A11 — `guides/monitoring-your-runs.mdx:3,6,34` and `platform/usage-and-billing.mdx:6` say Usage/Billing/Org Brain have no sidebar entry; all three are rail icons (`-organization-settings-nav.ts:220-224,243-264,251-264`), and only `/tasks`, `/sessions`, `/computers` are hidden | **confirmed, including the "wrong at authoring time" conclusion** | Doc lines 3, 6, 34 and `usage-and-billing.mdx:6` are verbatim. Source `:220-224` Org Brain entry with no hide flag; `:243-249` the comment "Both stay visible rail icons"; `:251-257` Usage and `:258-264` Billing, both `railPlacement: 'utility'`. `hideFromPrimaryNavigation: true` appears only in `computers-nav.config.tsx:20`, `sessions-nav.config.tsx:20`, `tasks-nav.config.tsx:22`. **`git log 41baea75..c3c16be6 -- -organization-settings-nav.ts` returns nothing**, so the file is unchanged since the snapshot and the error was indeed authorial, not drift. |
| 20 | §3.A — `guides/monitoring-your-runs.mdx:29` says the runners widget has a tooltip; it is now an `aria-label` (`sidebar-computer-capacity.tsx:22,44-48`) | **confirmed** | Doc line 29 verbatim. Source `:22` builds the `<idle> ready, …` string; `:44-46` is the comment "The row used to explain itself through a hover tooltip… survives here as the accessible name"; `:48` `aria-label={\`Beetle runners: ${description}\`}`. `Beetle runners` label at `:54`. |
| 21 | §3.A — `self-hosting/overview.mdx:53` "`free`, 5 seats" should read `RECEIPT_DEFAULT_FREE_SEAT_COUNT`, default 5 (`workspace-usage/shared.ts:27-34`) | **confirmed** | Doc line 53 is the table row verbatim. `shared.ts:27-30` parses `process.env.RECEIPT_DEFAULT_FREE_SEAT_COUNT`; `:31-34` falls back to `5`. |
| 22 | §2.2 / §6.1 A4 — guardrails are still unenforced: `applyGuardrails`, `guardPrompt`, `guardResponse`, `guardToolResult` have zero callers across `apps/start/src` and `packages/receipt-app/src` | **confirmed** | `git grep` for the four names across both trees at `c3c16be6` returns **only** their definitions in `guardrail-enforcement.service.ts` (`:82`, `:118`, `:122`, `:124`, `:127`, `:129`, `:132`). No call site anywhere. |
| 23 | §6.3 / §2.1 — the Model Gateway **Playground** is still a stub (`playground.tsx:9-16`, an `<h1>` only) | **confirmed** | The whole file is 17 lines; `PlaygroundPage` at `:9-16` renders a `div` containing a single `<h1>Playground</h1>`. |
| 24 | §5.1 — the twelve existing redirects and their destinations | **confirmed — all twelve source/destination pairs exact** | Parsed from `ed576e9`; the table reproduces row-for-row, including the four wildcards (`/connectors/:slug*`, `/regulation/:slug*`, `/essentials/:slug*`, `/api-reference/:slug*`). |
| 25 | §5.1 — the app's sidebar footer hard-codes `https://docs.kentron.ai/introduction` (`Receipt/apps/start/src/components/layout/app-sidebar.tsx:57`), so `introduction` must keep resolving | **confirmed** | `app-sidebar.tsx:57` = `const DOCS_URL = 'https://docs.kentron.ai/introduction'`. |
| 26 | §4 — frontmatter is exactly `title` + `description` on 49/49 pages, no other keys; every page (49/49) ends with a single `Next step:` line as its last line | **confirmed** | Scripted check over all 49 pages at `ed576e9`: 49/49 have exactly the key set `{title, description}` and nothing else; 49/49 have a final non-blank line beginning `Next step`. |
| 27 | §4 — 159 code fences; 113 internal links; tables on 39/49 pages | **wrong (minor, three counts)** — actual: **172** fenced blocks, **128** internal `](/…)` links (128 with code fences stripped too), tables on **37** pages | Same 49-page corpus at `ed576e9`. The direction of every error is an undercount; none changes a conclusion, but the numbers should not be quoted as measured facts. |
| 28 | §4 — eight Mermaid diagrams (`flowchart` ×5, `graph` ×1, `stateDiagram-v2` ×1, `sequenceDiagram` ×1 with an `%%{init}%%` sizing block at `develop/jobs-and-durable-execution.mdx:87`); one `classDef` using `#0D9373` at `guides/managing-conversations.mdx:20-21`; exactly one external link, `http://localhost:3000` at `repo/local-development.mdx:172` | **confirmed — every element exact** | 8 ```` ```mermaid ```` blocks: flowchart 5, graph 1, sequenceDiagram 1, stateDiagram-v2 1. `jobs-and-durable-execution.mdx:87` is the `%%{init: {'sequence': …}}%%` line. `managing-conversations.mdx:20-21` = `classDef canonical stroke:#0D9373…`. Exactly one `](http…)` link site-wide, at `repo/local-development.mdx:172`. |
| 29 | §6.1 A7 / §3.A — release builds bake `https://app.kentron.ai` and are smoke-tested for it with dotenv/bunfig autoload disabled (`build-receipt-cli-release.sh:9,73-76,106-107`); the error path is `receipt-connect-command-proxy.ts:51,616`; session reuse turns on `:381` | **confirmed — all citations exact** | `build-receipt-cli-release.sh:9` `HOSTED_GATEWAY_URL="${RECEIPT_CLI_DEFAULT_PROD_GATEWAY_URL:-https://app.kentron.ai}"`; `:73-76` the `env -i` doctor smoke test; `:104-105` `--no-compile-autoload-dotenv/--bunfig`; `:106-107` the two `--define` flags. `proxy.ts:51` `DEFAULT_RECEIPT_CONNECT_PROD_URL`; `:616` the "production URL is not configured yet" message; `:381` `sameNormalizedReceiptConnectUrl`. |
| 30 | §3.A — the Agent Registry's cloud connection is named `agent-registry` and is filtered out of Organization Settings → Integrations (`agent-registry-connection-name.ts:1-9`) | **confirmed** | The file is 9 lines; `:9` `export const AGENT_REGISTRY_CONNECTION_NAME = 'agent-registry'`, above it the comment explaining the separation from the "default" connection. |
| 31 | §7.3 — the Mintlify navigation-options table, "fetched 2026-09-08" from `https://mintlify.com/docs/navigation` | **unverifiable** | No network fetch was performed for this check and the page is outside both repositories. The *validator-side* consequences J1 draws from it (claim 9, and the `dropdowns`/`versions` gap) are confirmed independently from `check-docs.mjs:27`. |
| 32 | §6.1 A1, A3, A5, A6, A8, A9, A12 — items J1 itself marks "unverified" | **unverifiable (as J1 states)** | Left as-is; J1 correctly labels them rather than asserting them. Not counted against the report. |

## Leak scan — `J1-existing-docs-audit.md`

| Pattern | Hits | Assessment |
|---|---|---|
| 12-digit numbers | 1 (`:340`) | The sanctioned placeholder `123456789012`, quoted while describing the validator rule. **Clean.** |
| `arn:aws` | 0 | Clean |
| `i-[0-9a-f]{8,17}` | 0 | Clean |
| Public IPv4 | 0 | Clean |
| `/Users/<name>` | 5 (`:32`, `:33`, `:36`, `:98`, `:303`, `:349`) | All are the literal placeholder `/Users/you` quoted as the validator's finding, plus `/Users/me` as the allowed form. No real username appears. **Clean.** |
| `*.ts.net` | 0 | Clean |
| Given names of real people | 0 | Checked against the repo's actual commit authors and against the validator's own banned identifiers (`Satish`, `knsre`) — zero hits. **Clean.** |
| Internal deploy names (`beetle.run`, `beetle-deploy`, `AWS_PROFILE=beetle`) | 0 | Clean. Note J1 refers to "the marketing domain" and "two internal deploy names" descriptively without printing them — correct handling. |

**Verdict: no leaks.** J1 is safe to circulate as-is.

## Missing or too-thin topics

1. **The restructure that already exists on disk is not audited at all.** The doc working tree has 7 tabs, 29 groups, **81** page slugs, **58** redirects and a `navigation.global.anchors` block, plus 13 written `co-worker/` pages and 4 `mcp-gateway/` pages. J1 §5.2 and §"Suggested page split" propose a target that is roughly 40% built. The report needs a section reconciling its proposed slugs against the ones already chosen (e.g. J1 proposes `co-worker/conversations`, `co-worker/replay`, `co-worker/monitoring` — all three exist; but the tree also has `co-worker/how-it-works`, `objectives-and-tasks`, `skills-in-chat`, `teams` and `mcp-gateway/quickstart`, none of which appear in J1's plan, and `teams` contradicts J1's "still withheld" statement).
2. **`navigation.global` is a validator blind spot J1 did not anticipate.** §7.2 lists `dropdowns`/`versions`/`languages` as keys `collect()` misses, but the tree already uses `navigation.global.anchors` — also not in the `:27` key list. It happens to be harmless today because both anchors are external `href`s, but a `global` anchor that ever lists a page slug would be silently missed by the duplicate and `--complete` checks. Add `'global'` to the recommended key-list patch.
3. **No word/component budget for the 17 new pages**, so the §1.3 "reusability verdict" cannot be checked against what was actually written.
4. **The two post-`c3c16be6` commits are not covered.** `c87b2692` alone changes three things J1 asserts: the autonomy filter's behaviour, the workspace-invite "Invitation email sent" copy (J1 §3.A tells writers to document a Pending badge and Cancel action; the dialog now also surfaces an invitation link and states when mail cannot be sent), and the Skills surface. `d004ba19` adds the composer Skills chip.
5. **The "62 connectors / 89 tools" recount J1 defers is answerable now:** `packages/receipt-app/src/integrations/nango/catalog.json` holds **63** unique `providerSlugs` at both `c3c16be6` and `d004ba19`. §2.2 and open question 7 can be closed with that number rather than left open.
6. **No check that the proposed redirect map survives `check-docs.mjs:105-107`.** J1 notes the rule but does not test that its 30-odd proposed destinations are navigation pages; several (`core/accounts/your-account`, `cli/from-source/*`, `guard/compliance-policy`) do not exist in the working tree's `docs.json` yet, so adding them today would fail `--complete`.
7. **No accessibility or i18n note**, despite the report repeatedly turning tooltips into accessible names — the writing convention for "the accessible name is X" versus "the tooltip says X" is now a recurring editorial decision and deserves a line in §4.
